# Design completion audit

This audit maps the supplied `pcbex` design and subsequent extensions to
executable evidence. The project began with the deliberately bounded,
rectangular two-layer MVP and now supports polygonal multilayer signal boards,
coupled differential-pair routing, and native KiCad copper-zone generation.

| Requirement | Implementation | Evidence |
| --- | --- | --- |
| Rust core/CLI and Python agent separation | `pcbex-core`, `pcbex-kicad`, `pcbex-cli`, `pcbex_agent` | Workspace release build |
| Core module boundaries | Dedicated geometry, checking, placement, schema/migration, and quality-analysis modules with stable root re-exports | Workspace tests exercise schema and quality APIs through their unchanged public paths |
| Integer-nanometre model | `Point`, `Board`, `Footprint`, `Pad`, `Obstacle`, `Rules`, `Route`, `Via` | Serde models and round-trip tests |
| Versioned JSON contract | Draft 2020-12 schema, `schema_version`, v1 aliases, strict unknown-field/future-version diagnostics, and migrate CLI | Legacy width/height/signals migration and rejection regressions |
| JSON single-net routing and SVG | Multi-layer A* and `render_svg` | `routes_around_obstacle`, `svg_is_produced` |
| 8-way tracks and path simplification | Directional A* state and segment coalescing | Internal checker enforces H/V/45-degree tracks |
| Width and clearance | Inflated fixed obstacles and expanded committed routes | `separate_nets_keep_clearance` |
| Multiple nets and ordering | Priority, terminal count, and span ordering | `routes_ten_signal_nets` |
| Two layers and through vias | Layer state, via transition cost, circular via output | `changes_layers_with_through_vias_when_front_is_blocked` |
| Layer-specific obstacles and keepouts | Layer-owned raster cells; KiCad keepout import | `imports_copper_keepout` |
| Rip-up/reroute and failure report | Four bounded full rip-up passes with history cost | `reports_unrouted_after_bounded_reroute_passes` |
| Pad ownership and off-grid access | Net-owned obstacles and exact orthogonal access tracks | `foreign_net_pad_blocks_but_own_pad_is_enterable`, `connects_off_grid_terminals_exactly` |
| Internal rule checking | Copper-graph connectivity, orphan copper, angle, dimensions, boundaries, obstacles, copper clearance | Connectivity regression tests and `detects_cross_net_short`; every CLI route invokes it |
| Incremental routing | Preserve and reserve complete existing routes; route only missing nets | Idempotence, preservation, duplicate-route, and KiCad import regression tests |
| Exact geometry predicates | Integer point/segment/rectangle distance comparisons and inclusive segment intersection | Collinear overlap, endpoint contact, half-unit, and exact-clearance tests |
| Multi-terminal routing | Central-root selection and cheapest terminal-to-tree A* branching | Five-terminal cross costs 320 versus 512 for input-order chaining |
| Selective rip-up/reroute | Failed-search blocker attribution, conflict-only rip-up, failed-net-first retry, via reservation | Crossing regression reroutes only the flexible blocker; static blockage stops after one pass |
| Explicit local route repair | Checker-derived or user-selected net rip-up with byte-locked unrelated routes and owned-zone preservation | Obstacle violation regression reroutes one net while proving the other route is unchanged |
| KiCad input/output | Rectangular outline, rotated pads, footprints, tracks, vias, keepouts; board-level track/via output | Four `pcbex-kicad` tests |
| Bounded KiCad S-expression parsing | One typed, iterative parser serves PCB, schematic, manufacturing, placement, and custom-rule paths while enforcing fixed input, token, decoded-atom, nesting, direct-list-element, and span-result ceilings; quoted parentheses remain atoms and custom rules use a no-copy multi-root mode | Exact-bound and one-over unit tests cover every resource limit, malformed documents, quoted/escaped/Unicode atoms, deep nesting without recursion, multiple custom-rule roots, bounded span discovery, and public import/write-back compatibility |
| Fail-closed numeric and raster resource domain | KiCad physical values, routing-relevant copper layer sets, and supported pad geometry fail closed instead of using float-cast saturation or malformed-value defaults; shared core preflight uses checked wide arithmetic to bound board/grid/layer products, polygon and checker pairs, raster candidates and edge visits, segment/via clearance work, and atomic zone cells/blocker scans | Exact-bound and one-over injectable-limit tests plus JSON/KiCad regressions cover non-finite and out-of-range fields, malformed explicit geometry, huge boards with tiny grids, high-edge and high-blocker amplification, excessive topology/line work, clamped off-board zones, nonpositive dimensions, and atomic zone-fill rejection |
| Deterministic aggregate A* work budget | One checked budget spans coupled-pair, normal-net, retry, validation-reroute, and automatic-shove searches; it counts heap removals and successful relaxation insertions, allocates parallel first-pass work deterministically, and preserves the historical report-only fallback API | Exact-bound/one-over, shared coupled-plus-normal, atomic checked failure, worker-count determinism, and report-only fallback regressions cover the runtime ceiling without wall-clock assertions |
| Modern KiCad project/custom rules | Ordered wildcard/regex `netclass_patterns`, exact-assignment precedence, and NetClass-conditioned `.kicad_dru` routing dimensions | KiCad 9-style project fixture plus mm/mil custom-rule regression |
| Headless KiCad validation | `route-kicad --drc` | KiCad 10.0.5: 0 violations, 0 unconnected pads |
| Reproducible manufacturing package | Isolated `fabricate` DRC gate plus all-copper Gerber, paste/mask/silkscreen, drill, BOM/CPL, manifest, and ZIP export | Four-layer/Paste KiCad E2E, timestamp normalization, byte-identical rerun, metadata and polluted-output regressions |
| Semantic BOM/CPL package validation | One shared bounded RFC 4180 validator enforces exact BOM/CPL headers, embedded-newline-safe quoting, BOM quantity/type/layer/count semantics, and unique finite checked-decimal CPL references/layer/counts before publication or downstream acceptance; vendor coordinate transforms, BOM/CPL subset/order coupling, and Gerber semantic parsing remain outside this boundary | CSV parser, semantic mismatch, count, non-finite/overflow, duplicate-reference, malformed-quote, embedded-newline, factory-submit, feedback-candidate, pipeline, deterministic-runner, and no-schema-change regressions |
| Bounded manufacturing package and workspace | One quota contract bounds recursive manufacturing and repair workspaces, artifact collection, normalization lines, manifests/Gerber jobs, individual and aggregate bytes, compressed/expanded archives, cross-platform entry names, identity-checked source reads, and preflighted publication; generated ZIPs pass the factory validator before publication | Exact/one-over entry, depth, per-file, aggregate, compressed/expanded archive, manifest, line and name tests cover links, sockets, source growth/replacement, partial-output preservation, known-good repair fallback, reproducible archives, and real KiCad packaging checkpoints |
| Bounded factory quote and DFM connector | JLCPCB, PCBWay, and generic adapter identities verify every manifest-bound manufacturing artifact before submission through a no-redirect, compressed/expanded-size- and timeout-bounded HTTPS boundary; optional Bearer credentials remain environment-only while normalized receipts bind package/request/response digests and deterministic quote/DFM evidence | Real HTTP-stack loopback tests cover headers, credentials, payload and digest binding, producer-package compatibility, archive tampering, redirects, malformed/oversized responses, strict receipt schema, and fail-closed DFM gating; CLI integration atomically refuses receipt/schema overwrite before factory access |
| Bounded factory DFM repair loop | `factory-feedback-loop` permits at most four submissions in 900 seconds, caps the trusted deployment-owned repair child at 600 seconds, clears its inherited environment, rejects reflected Bearer credentials, and promotes a candidate from private staging only after closed manifest and complete manufacturing/Gerber-job validation; the last structurally valid package, failure report, and available matching receipt use atomic no-overwrite outputs distinct from the input, without claiming descendant-process sandboxing | Unit, real HTTP-stack, and CLI regressions cover deadline and attempt bounds, token non-inheritance/reflection, input immutability, complete candidate revalidation, known-good fallback, repair/submission failure reports, missing receipts after transport failure, and output collision/symlink/overwrite refusal |
| Hash-bound hardware pipeline gate | `pipeline-verify` recomputes electrical approval from the exact schematic and policy, binds closed analysis/check/quality evidence to the exact board, fully revalidates the selected final manufacturing ZIP, and verifies a closed hash-described firmware bundle against the same schematic identity; the five-phase report is an atomic no-overwrite digest manifest with no host input paths | Unit and CLI E2E cover a complete accepted chain, phase inconsistency and digest tampering, bounded input rejection, strict schemas, exact ZIP/board binding, failed-report retention, and output collision, alias, and symlink refusal |
| Factory-bound hardware pipeline gate | Supplying `--factory-receipt` or `--require-factory` emits the closed six-phase v2 report; the final phase strictly revalidates the normalized offline receipt, binds package/request size and SHA-256 to the single validated manufacturing ZIP snapshot, requires HTTPS, 2xx acceptance and an explicit DFM pass, and rejects every severity outside `info`, `notice`, or `warning` | Unit and CLI E2E cover exact receipt/ZIP binding, missing required receipts, closed schema and normalized-field mismatches, failed or ambiguous feedback, unknown severities, endpoint policy, bounded and symlink-safe receipt reads, retained failures, and output alias refusal while preserving the five-phase v1 contract |
| Interactive KiCad integration | Official `kicad-python` IPC adapter and one undoable commit | Mock transaction test and real wrapper-object serialization check |
| Placement scoring | HPWL, overlap, boundary, congestion, constraint penalties | Placement score improved from 560624 to 642 in the example |
| Placement search | Graph-clustered initial state, deterministic simulated annealing, move/rotate/swap, final snap | Placement unit tests |
| Placement constraints | `near` (including rotated named pins), `board_edge`, `keep_together` | Placement and agent tests |
| Natural-language planning | Deterministic English/Japanese parser plus schema-validated injected LLM adapter | Planner and LLM safety tests |
| SKiDL and part search boundaries | Optional SKiDL graph converter and injected catalog search | Adapter tests |
| Rust-gated natural-language circuit generation | A bounded shell-free provider emits only closed circuit-spec v2 candidates; exact raw JSON is normalized into deterministic schematic IR and accepted only after the native immutable ERC floor reaches zero errors under one aggregate deadline; the compatibility SKiDL renderer preserves explicit v2 no-connect pins with the native `NC` singleton while leaving schema-v1 bytes unchanged | Rust schema/conversion/ERC tests plus Python correction-loop regressions cover malformed, repeated, non-improving, oversized, timed-out, digest-inconsistent, and namespace-hostile candidates without publishing failed output; no-connect source execution and forged null/type, coverage, and cross-net relationships fail closed |
| Atomic saved-generation KiCad handoff | One stable-read schema-v2 generation bundle has its closed shape and every retained or reconstructable electrical/selection relationship revalidated, its provider/SKiDL fields remain inert, and its exact normalized spec is replayed through immutable Rust ERC, the deterministic KiCad writer, and explicit semantic handoff under one monotonic deadline; success publishes the exact source/check/schematic/report set plus a closed domain-separated manifest as one deterministic no-clobber ZIP, while AI, supplier/catalog provenance, native-KiCad, board, and manufacturing approval remain separate gates | Python regressions cover no-catalog and catalog-resolved evidence, digest/history/SKiDL forgery, duplicate keys before spawn, deadline-before-read, child failure, output collision, exact ZIP membership/metadata/digests, schema closure, and byte determinism; the KiCad E2E job exercises the real Rust checker/writer/handoff chain and inspects the published archive |
| Verified circuit handoff bundle consumer | One stable-read offline consumer requires the exact six-entry deterministic ZIP framing, fixed stored regular-file metadata, CRC and role byte limits, strict JSON, closed manifest/domain identity, and complete retained generation/spec/check/schematic/handoff graph before returning a path-free verification result or writing fixed names to a newly reserved no-clobber directory with `manifest.json` last; optional expected outer/logical digests provide external identity roots, while unsigned internal consistency is not producer or downstream approval | Python regressions cover verify/extract success and result schema, expected-root mismatch, duplicate/missing/extra/reordered/traversal names, compression/timestamp/link/special/extra/comment/framing/CRC/size rejection, duplicate/non-finite/noncanonical manifest and aggregate forgery, fully rehashed semantic inconsistency, source/output links and collisions, manifest-last order, write failure, and fsync rollback; KiCad E2E consumes a real Rust-produced archive |
| Exact circuit handoff chain replay | An explicit non-offline consumer completes the unchanged canonical archive and optional expected-identity verification, then reruns the retained generation source through the supplied bounded shell-free `pcbex` command under one aggregate deadline; catalog-input ERC is reconstructed and replayed when required, followed by final ERC, deterministic schematic writing, and semantic handoff, and success requires the complete manifest and ZIP to reproduce byte-for-byte while the supplied executable and real KiCad ERC remain separate trust boundaries | Python regressions cover non-catalog and catalog replay, closed path-free result/schema, expected-root rejection before child execution, a self-consistent forged schematic rejected by fresh reproduction, native failure, deadline expiry, source preservation, offline v1 compatibility, CLI routing, and real Rust handoff-chain replay in KiCad E2E |
| Native KiCad ERC-bound handoff replay | An optional retained native ERC v1 report or v2 report plus exact warning-policy sidecar is bounded and stable-read before producer replay, then privately staged and reread around a fresh `kicad-cli sch erc` run only after the canonical six-entry handoff reproduces exactly; a shorter Rust child timeout reserves Python cleanup time, and the closed path-free v2 result binds report/run/decision/count/policy identities while omission preserves the v1 replay result and archive contract | Python regressions cover v1 compatibility, v1/v2 identities and closed schema, policy-version firewall, rejected-evidence visibility and approval gating, report/policy mutation, oversized and option-like paths, summary/source forgery, aggregate timeout, CLI routing, and real Rust/KiCad replay; Rust parser/process tests cover fractional/default/invalid timeouts, retained-report preservation, direct-child reaping, and descendant termination |
| AI schematic quorum-bound handoff replay | An optional complete non-session schema-v1 request, organization policy pack, retained quorum report, and paired signed approvals/responses are bounded and stable-read before the unchanged six-entry producer replay; after optional native ERC, the existing Rust `verify-ai-quorum --schematic` gate runs over privately staged exact verifier inputs and reproduced schematic, requires a byte-identical retained report, rereads every source, and emits a closed path-free v3 result with explicit policy/count/decision/source identities before an optional final quorum gate, while omission preserves exact v1/v2 compatibility | Python contract and CLI regressions cover incomplete/mismatched sets, member/per-file/aggregate limits, schema-v1 firewall, explicit threshold validation, closed v3 with and without native evidence, exact v1/v2 compatibility, rejected-evidence visibility, report/input mutation and forgery, bounded execution, and final rereads; real Rust handoff E2E exercises signed multi-reviewer quorum reproduction without invoking a provider or accepting a private signing key during replay |
| Catalog-provenance-bound exact handoff replay | An optional all-or-nothing 1 MiB provenance, 1 MiB fetch-receipt, and 4 MiB snapshot set is accepted only for a catalog-backed archive, captured under a 6 MiB aggregate ceiling, and reread around the existing offline v1.421 validator after exact six-entry producer replay and any independently requested native/AI assertion; the closed path-free v4 result binds the complete validated historical graph plus source byte/SHA-256 identities, while omission preserves exact v1/v2/v3 results and no sidecar enters the archive | Python contract and CLI regressions cover incomplete sets, the non-catalog firewall, tampered/empty/linked sources, mutation after a later child, file and injected snapshot origins, closed v4 schema/scope/source identities with optional native and AI evidence, exact v1/v2/v3 serialization compatibility, aggregate deadline failure, historical offline operation without supplier/network access, CLI routing, and schema no-clobber publication |
| v1.454 retained-board electrical handoff replay | An optional retained `--kicad-board` plus exact `--board-binding-report`, with an optional exact custom replay-source `--board-binding-policy`, is stable-read under 128/12/4 MiB bounds, replayed only after the unchanged six-entry archive and prior optional assertions, and compared byte-for-byte through the existing geometry-free board-binding gate; the optional `--require-board-binding-approved` is a final gate; the closed path-free v5 `board_binding` result (scope `deterministic-electrical-handoff-chain-board-binding-replay-v5`) carries compact decision/counts and board/report plus raw replay-policy, effective-policy, electrical, handoff, and binding identities, while omission preserves v1–v4 result bytes and the archive | Python contract/CLI regressions cover incomplete sets, policy/approval gating, empty/link/oversize/mutation sources, private report output, compact-summary versus full-report stdout, exact report/board/policy rereads, geometry-only electrical-digest behavior, closed v5 schema/scope/path absence, exact v1–v4 serialization compatibility, aggregate deadline failure, and explicit non-claims for placement, routing, PCB DRC/DFM, manufacturing/fabrication, procurement, and tool provenance |
| v1.455 fresh manufacturing-package replay | Standalone Python API/CLI stable-captures one portable-basename board, retained manufacturing ZIP, optional explicit project/rules sidecars, and one optional built-in/external DFM or physical profile under per-source and 512 MiB aggregate limits; it stages exact bytes privately, invokes the selected pcbex/KiCad commands without a shell, gives Rust `fabricate` a strictly shorter aggregate deadline, and succeeds only when a freshly generated ZIP is byte-identical and every staged/caller source rereads unchanged; the closed path-free schema-v1 result uses scope `manufacturing-package-fresh-replay-v1`, while regenerated outputs remain private and pipeline/MCP/Action/fabrication authorization are unchanged | Python API/schema/CLI regressions cover closed successful results, external profile basename retention, all profile modes/exclusion, byte mismatch, staged/caller mutation, links and portable-name risks, per-source/aggregate ceilings, nested deadline reserve, malformed commands/deadlines and child failures, plus schema no-clobber output; Rust tests cover aggregate deadline capping/expiry and one explicit KiCad executable across DRC/export/identity; real KiCad E2E proves exact replay and one-byte retained-ZIP rejection |
| v1.456 fresh deterministic-pipeline report replay | Standalone Python API/CLI captures the exact plan, retained report, every present descriptor-exact source from the fixed 16-role contract, and an exact-eight firmware closure; reconstructs the original relative tree privately; invokes the selected pcbex command's existing deterministic runner without a shell; independently recomputes plan/run domain hashes; validates the complete closed nested binding/handoff/electrical/pipeline report graph with strict integer bounds; and accepts only exact retained/fresh report bytes including the final LF followed by complete staged/caller rereads. The closed path-free result uses scope `deterministic-pipeline-fresh-replay-v1` and keeps exact `verified` replay distinct from a downstream gate's reproduced `approved` decision; input-boundary rejection reports remain outside the safe staging contract, and no producer, KiCad/fabricate invocation, firmware build, MCP/Action parity, network/factory call, or authorization is added. The schema-v1 portable path pattern and shared runtime validator additionally reject `CONIN$`/`CONOUT$` device stems for safe cross-platform staging | Focused bounded-fake-child Python API/schema/CLI regressions cover approved and downstream-gate-rejected exact replay, closed/numeric nested-report forgery, report/plan/input/firmware mutation and mismatch, fixed relative staging, closed path-free results, approval gating after replay, input/report/argv/output/deadline bounds, child failures, final rereads, and schema no-clobber publication. The existing `Deterministic Pipeline` Linux job and macOS/Windows boundary matrix build real Rust binaries and replay retained accepted/rejected fixture reports; they cover scope, completed validation, verification, reproduced approval, byte identity, and retained report digest while existing Rust runner unit/CLI coverage remains responsible for deterministic report bytes and schema versions |
| v1.457 shared-board circuit-to-manufacturing replay | The existing circuit-handoff replay accepts manufacturing inputs only with a complete v5 board-binding pair, captures one raw KiCad board for both stages, reproduces the unchanged handoff archive and exact retained board-binding report before privately running the v1.455 manufacturing-package replay, cross-binds the nested board byte count/SHA-256 to the v5 identity, and accepts only an exact retained/fresh ZIP. One outer monotonic deadline includes a strictly shorter nested manufacturing budget, cleanup, the nested staged-source checks, and one final union reread of every caller-visible input. The closed path-free v6 result uses scope `deterministic-electrical-handoff-chain-manufacturing-package-replay-v6`; omission preserves exact v1–v5 serialization, and reproduction remains distinct from manufacturing authorization. The board-binding importer also follows KiCad's `20251028` numeric-to-name-only net boundary, preserves legacy ID/name and zone-name consistency, constrains connected fields to native ancestry, and treats quoted empty free tracks as the modern implicit no-net state | Focused Python API/schema/CLI regressions cover successful closed v6 output, mandatory complete v5 inputs, profile exclusion, exact ZIP mismatch and source mutation, the approval gate before `fabricate`, rejected-evidence visibility when the gate is omitted, forged shared-board identity, composition with native/AI/catalog evidence, a strict nested deadline, one-time PathLike freezing, exact legacy v5 bytes, closed-schema forgery rejection, and schema no-clobber publication. Rust parser regressions cover legacy/modern semantic-digest equivalence, dialect mixing, quoted-token differentials, routed/zone/graphic-only nets, ancestry smuggling, zone `net_name`, and empty modern tracks; the real KiCad 10 E2E composes the upgraded shared board with exact retained/fresh manufacturing ZIPs |
| v1.458 pipeline-bound circuit-to-manufacturing replay | The complete v6 replay optionally accepts one all-or-nothing deterministic plan/report pair, captures the plan/report/all selected roles/exact-eight firmware closure before any producer, replays the pipeline last, and requires exact circuit/schematic/raw-board/manufacturing-ZIP identity, effective policy, the complete canonical nested board-binding report, and canonical schematic/raw-board pipeline identities. Approved reports additionally require strict supplied-review and board-basename agreement, while truthful downstream rejection evidence remains visible by default. Ordered manufacturing/pipeline subdeadlines and a final union reread plus firmware-directory closure share the original outer deadline. The closed path-free v7 scope is `deterministic-electrical-handoff-chain-manufacturing-pipeline-replay-v7`; omission preserves v1–v6 serialized bytes and no factory, procurement, fabrication, network, MCP, or Action authority is added | Focused Python API/schema/CLI regressions cover closed accepted and rejected v7 results, complete preflight, every shared identity and full nested-binding mismatch, approval forgery versus rejected review/name evidence, exact v6 omission bytes, staged/caller/late firmware mutation, PathLike freeze before adversarial command normalization, per-file staged-reread deadlines, strict nested subdeadline, closed-schema flags/forgery, and no-clobber schema publication; standalone v1.456 and v1.457 compatibility suites remain green, with real deterministic/KiCad composition covered by CI/E2E |
| v1.459 dual-control exact fabrication release authorization | A factory-required deterministic plan is freshly replayed in-process and must reproduce one retained `approved: true` report byte-for-byte; the exact manufacturing ZIP, passing normalized factory receipt, and plan-selected policy pack are then independently revalidated before dedicated disjoint human keys sign raw/canonical evidence plus a bounded authorization ID/challenge/quantity/currency/value/window scope. At least two valid submitted approvals and no submitted rejection are required; the closed audit snapshot retains the complete pack and signer-sorted signed envelopes, while valid quorum/time/rejection failures remain truthful `not_authorized` evidence before an optional final gate. Consumption requires another fresh verification from the original artifacts and decisions; the result is not an outer-signed timestamp, factory authenticity, one-time consumption, submission, ordering, payment, spend, or fabrication execution | Rust core/schema/policy regressions cover domain separation, exact evidence capture, role/key overlap, signing preflight before key use, signature/evidence/scope tampering, distinct quorum, rejection/insufficient/inactive/policy-window outcomes, full signature verification during fresh evaluation, closed schemas, and policy-pack omission compatibility; CLI E2E covers an approved factory-bound plan, two-key authorization, retained rejection evidence, exact artifact mutation, private-key preflight ordering, firmware-directory output-boundary checks, no-clobber outputs, and schema/capability discovery |
| v1.460 MCP fabrication authorization verification parity | The existing fresh verifier is available synchronously and through optional MCP Tasks with the original plan/report/package/receipt/policy/approval inputs, a child-sampled evaluation time, and the same no-clobber 128 MiB report ceiling; signing, private keys, scope construction, network/factory submission, ordering, payment, and one-time consumption remain outside MCP. The complete policy and signed envelopes stay in the retained file. MCP returns a closed 23-field snapshot only after authenticating the exact report bytes/SHA-256, decision, scope and counts, plan/run digests, and raw manufacturing-package/factory-receipt/policy-pack source SHA-256 values. The snapshot deliberately omits receipt provider/endpoint/quote digest and policy canonical digest/ID/revision; its `evaluated_at_unix` preserves one evaluation instant but is not reusable current authority. A cancelled/expired Task never returns a successful authenticated summary; an atomic report that won the cancellation race may remain and still requires fresh replay | MCP unit and real-stdio regressions cover the closed tool schema and annotations, explicit absence of a signing tool or private-key input, Task allow-list/lifecycle, typed and unknown arguments, stale-output preservation, strict 23-field summary and retained-report authentication, authorized synchronous verification, retained quorum-shortage Task evidence, signature tampering with no output, sensitive-field omission, and representative responses below 16 MiB. The 128 MiB report ceiling and 16 MiB frame ceiling are separately enforced bounds; this milestone does not claim a near-limit 128 MiB adversarial authorization E2E |
| Deterministic KiCad schematic writer | Immutable-ERC-approved circuit-spec v2 is normalized into a bounded, self-contained flat/single-unit `.kicad_sch` with synthetic embedded symbols, fixed geometry, stable UUIDs, explicit labels/no-connects, and retained component metadata; exact generated bytes are re-imported and must pass the existing semantic handoff before atomic no-clobber publication | Writer and CLI regressions cover order-independent byte identity, import/semantic/metadata round trips, escaping, label and library-signature ambiguity, coordinate/output limits, ERC rejection, input alias, overwrite, and symlink refusal; KiCad 10 independently exports a native XML netlist whose complete connection sets are compared with the fixture |
| v1.461 focused fabrication authorization verification Action | A standalone boardless composite Action accepts the original plan/report/package/receipt/policy plus 1–100 signed approvals, runs only the fresh verifier, authenticates the exact 23-field compact bridge against one fixed no-clobber report, snapshots and final-rereads direct inputs, scans one depth-one 128 MiB evidence file, optionally uploads it, and applies `require-authorized` only in the final gate. Signing, private keys, scope construction, time override, fabrication-API submission, ordering, payment, one-time challenge consumption, and current reusable authority remain outside the Action; policy/envelopes/reasons/tickets remain in the confidentiality-sensitive full report | Manifest/helper/runner/gate tests cover the exact input/output and no-signing surface, newline approval bounds, path/no-clobber safety, attached CLI argv, strict duplicate-free report/summary correspondence, authorized and retained `not_authorized` decisions, input/report mutation, one-file artifact limits, upload-before-gate order, pinned upload action, sensitive metadata omission, and real factory-bound authorized/quorum-shortage CI smoke. The 128 MiB ceiling is enforced but near-limit upload is not claimed |
| v1.462 local fabrication authorization challenge reservation | A Unix-only CLI freshly requires an authorized exact fabrication report before writing a path-free five-key marker whose nested 23-field summary binds the in-memory full report while keeping `challenge_one_time_use_enforced: false`. The caller must select an absolute pre-existing effective-UID-owned mode-`0700` ledger, its fixed closed 4 KiB manifest, and expected ledger ID. One complete at-most-16-KiB challenge-keyed marker is synchronized, installed without replacement relative to the pinned ledger descriptor, followed by directory synchronization, and bracketed by local validity-window checks; existing entries, expiry after installation, and ambiguous post-install failures burn rather than reopen the challenge. This is cooperative local at-most-once admission, not global one-time use or a permission boundary without a credential-holding executor, and does not cover same-UID/admin mutation, ledger replacement/rollback, separate roots/hosts/runners, Windows/network filesystems, time/revocation/rejection discovery, factory authenticity, ordering, payment, or exactly-once side effects | Closed manifest/marker/schema and fresh-authorization regressions cover expected ledger identity, authorized compact binding, unauthenticated quote and false one-time flags, invalid/not-authorized evidence without a marker, deterministic filename, malformed manifest, insecure mode, symlink-root rejection, pre-existing entry types, concurrent same-challenge single-winner behavior, descriptor-pinned no-replace installation, pre/post-install guard failures, and final-marker retention after injected post-install failures. Existing MCP and focused/root Action contracts remain unchanged |
| v1.463 deterministic circuit-to-KiCad board producer | A CLI-only producer captures one circuit-spec v2, its exact KiCad schematic, a bounded embedded footprint closure, a closed construction/stackup profile, and the existing physical profile; reruns the immutable circuit/schematic handoff; instantiates exact pad geometry; performs deterministic physical-profile placement; reimports the newly rendered board; and requires the existing circuit/schematic/board binding to approve before atomically publishing an exact three-file placed-but-unrouted bundle. The path-free manifest binds every raw/canonical input, final board, retained binding report, and placement result. Host libraries, supplier/datasheet authenticity, routing, native DRC/DFM, manufacturing, authorization, procurement, network access, MCP, and Action remain separate | Parser/writer/schema and CLI regressions cover duplicate-free closed profiles, source byte/SHA and exact footprint/pad inventory, injected instance/net rejection, bounded stackup alternation/thickness/routing/placement values, deterministic board/manifest bytes, physical fixed placement and bounds, final approved board binding, mutation/alias/link/no-clobber failure, exact output membership, and real KiCad 10 plus downstream route consumption |
| v1.464 exact final-BOM procurement intent | A Rust CLI verifier fully validates one exact manufacturing ZIP, regenerates canonical `bom.csv` bytes from the exact supplied board with the production renderer, and retains a closed path-free approval or semantic-mismatch report. A Python API/CLI then fully replays the exact catalog-backed generation receipt against its captured snapshot, requires complete populated reference/value/footprint/MPN equality, non-null unambiguous supplier part numbers, and emits only approved per-board line items grouped by exact MPN/SKU/catalog-part/footprint identity. Rejection evidence has no partial lines; procurement authorization, live stock/pricing/MOQ, supplier authenticity, network access, reservation, ordering, payment, electrical implementation, and fabrication approval remain explicit nonclaims | Rust unit/integration/schema/capability regressions cover complete ZIP validation, shared canonical BOM rendering, exact raw identities, retained mismatches, portable basenames, bounds, no-clobber publication, and the final gate. Python regressions cover full receipt recomputation, strict nested-report authentication, reference-subset/empty/swap truthfulness, null/ambiguous SKUs, forged receipts and JSON types, stateful injected mappings, role-name collisions, mutation/final-reread order, deadlines/process bounds, closed schemas, retained rejection, and a real approved Rust bridge; real KiCad E2E cross-binds the retained manufacturing package and preserves the intentionally unrelated catalog mismatch as rejected evidence |
| v1.465 exact final-CPL board-bound placement evidence | A Rust CLI verifier fully validates one exact manufacturing ZIP, regenerates canonical `cpl.csv` bytes from the exact supplied board with the production fabrication plan/renderer, and requires both exact CPL bytes and package board-source byte-count/SHA-256 identity. Its closed path-free report retains at most 256 reference-sorted in-position parts with exact integer-nanometre X/Y, milli-degree rotation, and side plus truthful canonical/source mismatches. The basename is informational; circuit-authored placement, tool/vendor authenticity, factory coordinate transforms, panelization, assembly/machine execution, fabrication/procurement authority, network access, reservation, ordering, payment, and spend remain explicit nonclaims. Capture and final reread are sequential change detection, not a same-principal atomic snapshot | Rust unit/integration/schema/capability regressions cover production-renderer reuse, complete package validation, exact raw identities and placement fields, retained semantic mismatches, portable basenames, bounds, no-clobber publication, final gating, and observed mutation. macOS/Windows CI runs the focused release-mode verifier boundary suite. Real KiCad 10 E2E verifies the freshly fabricated board/package pair, closed schema/report invariants, exact source/CPL identities, and retained rejection for a fully valid package whose canonical placement differs |
| v1.466 fresh exact firmware-bundle build verification | A standalone Rust CLI captures one exact-eight manifest-v2 bundle, verifies exact ordered source descriptors, reconstructs only the seven source artifacts privately, ignores historical manifest argv when selecting work, and runs six fixed C11/C++17/Python compile and smoke checks. Its closed path-free `fresh_firmware_bundle_build_v1` report binds the manifest and seven source identities, process limits, ordered typed outcomes, explicit false toolchain-provenance flag, and approval decision. Valid negative build evidence is retained before the optional gate; malformed/unsafe/inexact/mutated input, cancellation, and unsafe output produce no report. Capture/publication is sequential rather than a same-principal race-free transaction. The executed tools, smoke binaries, and `host.py` require a trusted bundle/toolchain or separate OS sandbox, and the result adds no provenance, reproducibility, target-MCU/hardware-safety, cross-compilation, pipeline/fabrication composition, MCP/Action, procurement, or order claim while all prior schema versions and serialization contracts remain unchanged | Rust core and CLI integration regressions cover closed schema/field order, exact identities, source-only historical evidence, six real fresh checks, forged historical argv non-execution, typed compile/spawn/missing-output/dependency failures, exact-eight/hash/size and Unix symlink/special rejection, path-free output, no-clobber/alias boundaries, retained rejection before the gate, cancellation/no-report, final reread mutation rejection, and capability discovery. The existing macOS/Windows release matrix runs the focused `firmware_build` suite once, after Windows GNU toolchain PATH configuration; both hosts cover schema, real fresh checks, hash rejection, missing-tool retention, exact-eight failure, no-clobber, rejection retention, and schema publication. Timeout and link/special cases are Unix-only, final mutation is Linux-only, and no new Windows Job cleanup/descendant runtime regression is claimed. Static policy prevents duplication in the separate Windows Rust job |
| v1.467 exact per-board assembly evidence composition | A Python-only composer freshly requires the existing schema-v6 handoff, retained board-binding, shared-board, and manufacturing-package replay; semantically and fully replays one retained procurement intent from the exact archived generation entry plus supplied historical snapshot; byte-replays one retained final-CPL report; and hard-cross-binds shared board/package, handoff-generation, manifest, and final-BOM/final-CPL package-board-source identities into closed path-free `offline-exact-board-assembly-evidence-v1` evidence. The full procurement/final-BOM graph is construction-time validated, while the retained compact `final_bom` projection omits per-part records and the compact procurement projection omits nested final BOM plus its non-recomputable original binding digest; exact raw procurement identity, full replay, and the outer binding remain. The standalone renderer validates retained closed shapes and self-contained correlations, including the reconstructible full final-CPL raw identity; opaque raw/omitted child correlations and caller-input equality require fresh evaluator/validator replay, and neither path authenticates producer tools. Populated BOM references remain only in membership and approved procurement lines; the exact nested final-CPL evidence separately retains its coordinate-bearing reference records. Completion is exactly the conjunction of positive board-binding, procurement, and CPL decisions, while the BOM/CPL membership partition is informational and non-gating. Valid incomplete evidence is retained before the final gate; malformed/forged/mismatched/mutated input produces no report. Constant-false readiness/authorization/network/order/machine flags, per-board quantity, and explicit nonclaims prevent composition from asserting BOM/CPL subset completeness, vendor transforms, polarity/fiducials/panel/machine setup, batch/yield/spares, current supplier facts, ordering/payment, provenance/sandbox, atomic snapshot, manifest filename, or firmware | Focused Python API regressions cover closed canonical compact results, every child-decision outcome, non-gating membership, one generation cross-binding rejection, strict final-CPL numeric parity, nested deadline reserves, observed mutation/final rereads, canonical path/bytes validation versus bounded Mapping snapshots, and self-contained-correlation forgery/size rejection. Focused CLI regressions cover the exact option surface, profile exclusion, no-clobber publication, schema output, and gate-after-retention behavior; the existing child suites remain responsible for their complete replay and input/process-limit contracts. The macOS/Windows Python boundary matrix runs both focused v1.467 API/CLI modules once. Separately, real KiCad 10 E2E uses the release pcbex binary to run the full handoff/manufacturing, procurement, and final-CPL composition for one catalog-matched complete result and to retain one truthful incomplete result before its final gate |
| v1.468 exact offline supplier-offer coverage | A Python-only evaluator captures one normalized local offer, one exact retained procurement intent, and the intent's exact board/package/generation/snapshot closure; hard-binds the raw intent digest before execution; freshly reruns the complete public v1.464 validator; multiplies only the intent's per-board quantities by one explicit requested-board count with checked integer arithmetic; and retains closed `covered` or `not_covered` component-line evidence under a single outer deadline and final source rereads. Offer lines contain only exact MPN/SKU/historical catalog-part identities, quoted quantities, and fixed 10^-6 major-currency integer subtotals. Valid upstream-approval, supplier, line-set/identity, quantity, or half-open-window mismatches remain evidence before an optional gate, while malformed/misbound/unsafe/aliased/oversized/mutated/unreplayable input produces no report. Constant-false currentness/authenticity/network/reservation/authorization/order/payment flags and explicit nonclaims prevent coverage from asserting unit-price or rounding truth, MOQ/tiers/order multiples, shipping/tax/duty/fees/discounts/landed cost, trusted time, supplier origin, stock reservation, order readiness, or any side effect | Focused Python API regressions cover exact covered and each retained-negative outcome, strict offer/schema/runtime arithmetic and source binding, canonical rendering/retained validation, limits, aliases, mutation, and deadline behavior. Focused CLI regressions cover the exact command/schema surface, preflight/no-clobber publication, one-LF rendering, and gate-after-retention. The macOS/Windows Python-boundary matrix runs both focused v1.468 modules once; real Linux KiCad E2E reuses an exact approved procurement fixture for one covered offer and one retained shortfall before the final gate |
| v1.469 bounded supplier-offer HTTPS acquisition receipt | A standalone Python adapter preflights two distinct new destinations, performs one bounded no-redirect GET for the unchanged normalized-offer contract, requires exact caller-declared supplier and procurement-intent digest binding, and publishes canonical offer bytes followed by a closed receipt. One monotonic network deadline covers bounded secret-free DNS, TCP, platform-default TLS, headers, and body reads; response framing/media/encoding/header/body limits, strict JSON normalization, bearer-token non-retention plus exact entity-body/canonical-offer reflection checks, and atomic no-clobber publication fail closed. The receipt retains a credential-free domain-separated request digest plus exact entity-body and canonical-offer identities, sets only its local network-observation flag true, and keeps all currentness/authenticity/reservation/authorization/order/payment claims false. It retains no raw response or TLS evidence, cannot authenticate its own historical network fields during offline replay, and leaves all v1.420/v1.468 schemas and bytes unchanged | Focused Python API regressions cover real local transport, request/response/offer identity, strict endpoint/header/framing/status/size/deadline/token/offer rules, exact receipt/schema/canonical replay, source/path/alias/mutation and two-output race behavior. Focused CLI regressions cover the frozen option surface, hidden test controls, defaults, digest-only output, compact secret-free failures, output preflight, schema publication, and public exports. The macOS/Windows Python-boundary matrix runs both focused v1.469 modules once; real Linux KiCad E2E acquires the offer through a test-only local TLS endpoint and feeds its exact canonical bytes into unchanged v1.468 coverage while asserting acquisition-network true, coverage-network false, and every authenticity/currentness/side-effect flag false |
| v1.470 exact assembly and acquired supplier-offer evidence composition | A new offline Python-only composer captures the complete source union once, extracts the exact handoff generation entry rather than accepting a second generation path, validates the unsigned acquisition receipt against the canonical offer without network access, and freshly validates the full retained v1.467 assembly and v1.468 coverage children from the same private staged board/package/snapshot/intent/offer closure under one outer deadline. It retains all three full child objects, canonical child-source identities, and a new domain-separated binding; hard-cross-binds the named board, package, handoff generation, snapshot, raw intent, identical compact procurement projections, exact offer/receipt identities, caller selectors, and untrusted coverage/fetch timestamp equality; preserves the false/false/true nested network split plus outer false; and final-rereads both the staged and caller-visible unions. Completion is exactly assembly complete and offer covered, while valid child negatives are retained before the optional gate and malformed receipts, replay/binding mismatches, unsafe/oversized/aliased inputs, deadline/cleanup failures, or observed mutation produce no result. Full constant-false currentness/authenticity/reservation/readiness/authority/order/payment/machine fields and explicit nonclaims prevent timestamp correlation, requested-board quantity, component subtotal, or the receipt observation from becoming current supplier evidence, batch assembly evidence, landed cost, or authority; all v1.467–v1.469 schemas and bytes remain unchanged | Focused Python API regressions cover full canonical/schema/binding output, both retained-negative classes and their sorted union, every shared source/projection/offer/receipt/timestamp/network cross-binding, exact handoff-generation extraction, public child validation order and shared staged bytes, nested deadline reserves, path/alias/size/Mapping bounds, child/caller mutation and final union rereads, and standalone-renderer forgery rejection. Focused CLI regressions cover the exact offline option surface, profile exclusion, output preflight/no-clobber publication, schema output, one-LF bytes, and gate-after-retention behavior. The macOS/Windows Python boundary matrix runs both focused v1.470 modules once; real Linux KiCad E2E composes the existing real assembly, TLS-acquired offer/receipt, and covered report into one complete result and separately retains and gates one exact acquired shortfall result while checking full identities and false/false/true/false network semantics |
| v1.471 dual-control exact procurement release authorization | A Python-orchestrated boundary captures one retained v1.470 report plus its complete original closure, a mandatory externally expected canonical policy digest, one dedicated organization procurement policy, and bounded signed approvals; freshly validates the exact v1.470 result both before and after a separate trusted Rust cryptographic child; and alone constructs a closed path-free public report after exact pre/post replay equality and final staged/caller rereads. The domain-separated approval binds the raw v1.470 identity and outer binding, complete/covered commercial projection, supplier/offer/currency/window/requested-board/component-subtotal/receipt-observation values, policy raw/canonical/ID/revision identity, authorization ID, 64-hex challenge, signed component-line ceiling, inclusive offer-contained window, decision, signer, reason, and ticket. Its exact typed compact preimage additionally signs schema, scope, algorithm, and derived policy-matched public key; procurement policy rejects weak Ed25519 keys. At least two distinct role-disjoint Ed25519 signer IDs and keys—not necessarily distinct natural people—must satisfy the pinned policy without a submitted valid rejection. Valid upstream, quorum, rejection, local-window, receipt-observation-age, subtotal, signed-ceiling, and policy-duration failures retain truthful `not_authorized` evidence before the final gate; malformed, mixed, unpinned, invalidly signed, aliased, oversized, or observably mutated material produces no report. Only the outer procurement-authorization claim can be true; all network/currentness/authenticity/trusted-time/reservation/assembly/fabrication/order/payment/machine/one-time-use fields and all landed/invoice/shipping/tax/MOQ/tier/spend implications remain false | Rust golden vectors pin the exact request binding, full approval-envelope signature preimage, and verification key. Focused Rust policy and cryptographic tests cover dedicated-policy bounds and role/key separation including weak keys; exact request/source/pin validation; sorted positive quorum and self-validating assessment binding; all nine gate-failure forms, valid signed rejection, and component-subtotal failure; and signature, duplicate signer, envelope-context, cross-binding, offer-window, timestamp, unknown-field, and duplicate-key rejection. All 37 focused Python regressions pass (22 API and 15 CLI). The API suite covers exact request/report binding, ordering, and nonclaims; two fresh replays around the trusted child; retained historical validation across path, bytes, and one-pass Mapping representations; baseline and approval-hook capture order; deep-copy replay inputs; final pre-spawn rereads of every staged signing and assessment input; exact submitted-approval matching in the returned assessment; staged and caller-source mutation rejection; forged-helper rejection; recursively closed and bounded schema/runtime parity; CWD restoration; monotonic rollback; command/key aliases; Python private-key non-access; and incomplete/uncovered approve refusal before key freeze or child execution. The CLI suite covers the frozen sign/verify/schema surfaces and routing, distinct replay/authorization executables, profile exclusion, source/private-key/command/output alias preflight, no-clobber races, compact non-leaking failures, canonical one-LF publication, and gate-after-retention. The macOS/Windows boundary matrix runs both focused v1.471 modules exactly once before Windows GNU setup. The real Linux KiCad E2E passed and exercises the exact v1.470 real KiCad/TLS complete and shortfall closures, two release-binary approval keys, a canonical pin from a verified signed-policy envelope, recursive schema audits, an authorized two-signer quorum, an exact retained/gated single-approval negative, shortfall refusal before key access, all authorization nonclaims, and tampered approval/source hard failures without public output |
| v1.471.1 task-oriented documentation architecture | The root README is a compact project entry point with action-led features, a runnable quick start, representative JSON/KiCad/manufacturing/agent examples, configuration and component maps, and direct routing into five task-oriented guides plus one complete documentation index. The categorized hub indexes every focused contract, while dedicated getting-started, workflow, architecture, integration, and trust guides explain navigation and ownership without duplicating exact schemas, limits, replay rules, or nonclaims | `test_documentation_navigation.py` caps the README at 32 KiB/400 lines, requires its stable navigation sections and guide links, verifies every repository-relative Markdown target stays inside the repository and exists, and requires `docs/README.md` to list every focused Markdown document; the documented JSON route/check, KiCad route/native DRC, Python SKiDL, capability inventory, and GitHub Actions YAML examples were also exercised or parsed before release |
| v1.472 local procurement-authorization challenge reservation | `pcbex-agent reserve-procurement-authorization` freshly validates one retained authorized v1.471 report from its complete original v1.470 source closure and exact submitted approvals before deriving a compact canonical marker. A hidden Rust helper then pins one pre-created absolute Unix ledger, requires effective-UID ownership and exact mode `0700`, admits only reviewed local filesystems, verifies an independently expected ledger ID against the fixed closed manifest, rejects both lexical and canonical overlap with every replay input, stable-reads the staged marker, and installs the deterministic challenge name through descriptor-relative no-replace publication with file and directory synchronization. An existing destination burns the challenge regardless of its bytes. Authorization, offer, and receipt-observation time gates are rechecked before and after installation and after durable completion. The marker proves only local at-most-once admission inside that one ledger: network access, global one-time use, supplier authenticity/current availability, inventory reservation, order placement, and payment remain explicitly false, and ordinary v1.471 report bytes remain unchanged | Four Rust marker/manifest tests pin canonical parsing, deterministic naming, authorized/timing constraints, and recursively closed schemas; one anchored-I/O regression fails closed for remote, FUSE, tmpfs, and unknown Linux filesystem types; three real-binary integration tests cover public schema discovery, hidden-helper omission, exact private `0600` marker publication, same-name and corrupt-marker burn semantics, insecure-ledger rejection, direct input overlap, and a ledger-internal outward symlink. Eight focused Python API/CLI tests cover exact report bytes/SHA projection and nonclaims, negative-report refusal, private staging and helper argv, stage mutation, burned-challenge mapping, Unix/Windows gates, whole-deadline allocation, protected paths, fresh validation before commit, and no marker for negative authorization. The macOS/Windows boundary matrix runs both v1.472 modules and the Rust integration test, while the real Linux KiCad E2E replays the complete v1.471 fixture, commits one marker, rejects repeated/corrupt collisions without mutation, audits both closed schemas, and proves a single-approval negative creates no marker |
| v1.473 deterministic multi-unit KiCad handoff | The opt-in closed circuit-spec v3 keeps one physical part per reference, nests bounded explicit symbol units, and binds every net terminal to `(reference, unit, package pin)`. Normalization rejects duplicate units, cross-unit physical-pin reuse, per-package physical-pin overflow, incomplete coverage, and ambiguous connections before running the unchanged immutable electrical floor. The deterministic writer embeds unit-specific KiCad definitions and instances; the handoff verifier compares exact unit-aware symbols, metadata, pins, labels, and nets. Board binding and board production collapse only a fully validated v3 graph into the unchanged one-footprint physical model, while retaining the original v3 canonical digest. Existing circuit-spec v2 documents and flat-symbol workflows remain migration-free; version-stamped evidence continues to identify the producing release. Provider generation, hierarchy, automatic unit reassignment, hidden shared pins, alternate conversions, installed-library discovery, and datasheet/footprint authenticity remain outside this milestone | Rust unit and CLI integration tests cover required nullable fields, bounds, closed schemas, physical-pin ambiguity, deterministic unit-aware write/reimport, exact handoff, native-source identity, and schema discovery. Board-writer integration proves two U1 schematic units produce one QFN footprint, an approved board binding, and a manifest bound to the original v3 digest. The release KiCad 10 E2E exports a native netlist, requires one physical U1 component with both cross-unit terminal sets, recursively audits both v3 schemas, and passes the same deterministic board producer and board-binding gate |
| v1.474 bounded deterministic routing convergence | An explicit `--convergence-report` opts JSON or KiCad routing into a multi-round strategy portfolio while omission preserves the existing single-pass path. Stable balanced, shortest, via-minimized, bend-minimized, and alternate-order candidates receive deterministic shares of one aggregate production A* ceiling. Each successful candidate restores the exact input rules and runs the authoritative checker; only candidates whose findings are empty except for explicit `unrouted` nets enter the fixed validity-first ranking. A strict improvement alone becomes the next round input. The closed path-free report binds canonical effective-input and selected-Board bytes/SHA-256, every strategy allocation/status/metric/duplicate/selection decision, and the terminal outcome. Its explicit destination rejects input/output aliases and publishes atomically without replacement; valid partial evidence is retained before the unrouted gate. No physical/electrical rule is relaxed, and the report does not claim global optimality, raw-source authenticity, native KiCad DRC, manufacturability, or release authority | Focused core tests cover exact aggregate allocation, DRC-invalid candidate exclusion, deterministic parallel output, unchanged rules, already-routed and invalid-input boundaries, bounded options, and recursively closed/bounded schema structure. Focused CLI tests cover byte-identical repeated JSON Board/report output, KiCad parity, schema discovery, retained no-candidate evidence before the gate, explicit partial acceptance, occupied/aliased report refusal, and rejection of tuning flags without an opt-in report. The release KiCad 10 E2E repeats a real routed Board/report pair byte-for-byte, passes native DRC, recursively audits the schema, and retains a bounded no-candidate report before the gate; the full core, CLI, corpus, Python, and release suites preserve unchanged single-pass behavior |
| v1.475 fresh exact routing convergence verification | Separate JSON and KiCad consumers capture the complete selected raw source closure, reject unsafe or cross-role aliased paths, strictly parse duplicate-free inputs, require canonical retained report bytes, reconstruct the effective internal Board, and freshly replay the complete schema-v1 convergence decision while preserving its producer version. The freshly selected Board must regenerate the supplied routed JSON or KiCad artifact byte for byte. One closed path-free 32 MiB outer report retains source byte/SHA-256 identities, the full nested decision, six completed checks, and a domain-separated binding. Every caller source is reread before synchronized no-replace publication. Valid complete, partial, and no-candidate outcomes remain evidence; the latter two are retained before an optional complete gate, while malformed, noncanonical, substituted, changed, oversized, or publication-invalid input produces no report. Source authenticity, native KiCad DRC, manufacturability, and release authority remain explicitly false | Core regressions reproduce an older retained producer version, reject report-field substitution, and retain an exact bounded partial replay. The verification module recursively audits its merged convergence schema, including closed objects, bounded arrays, and role-specific source limits. Focused real-binary CLI tests cover JSON and KiCad positive replay, raw identities, false claims, canonical one-LF output, schema discovery, partial gate-after-retention and ungated byte equality, routed/report/duplicate/noncanonical substitution, output preservation, and source/output alias rejection. The macOS/Windows boundary matrix runs the focused verifier test immediately after v1.474. The release KiCad 10 E2E passed while repeating complete verification byte-for-byte, retaining and gating the no-candidate result, auditing exact identities and schema closure, and rejecting routed/report substitution without output |
| v1.476 fresh routing-to-manufacturing handoff | A Python-only composer captures one original KiCad board, routed board, retained convergence and v1.475 verification reports, retained manufacturing ZIP, optional project/rules, and exactly one built-in DFM, external DFM, or physical-profile selection under a 688 MiB source-union ceiling. It freshly invokes the existing routing verifier and requires exact retained bytes, validates the nested decision and source projection, then passes the same captured routed board and sidecars to the existing manufacturing replay only when routing is complete. One closed path-free 4 MiB report binds every source, the routing projection, the full manufacturing replay when present, seven validation flags, and one domain-separated digest. Valid incomplete routing skips fabrication and is retained before the optional ready gate; malformed, aliased, substituted, observably changed, expired, or child-mismatched input produces no public report. Source/tool authenticity, separate native DRC evidence, manufacturability, fabrication approval, external submission, and release authority remain false or outside scope | Fourteen focused API/CLI regressions cover the ready and retained-incomplete paths, exact board/package/profile cross-binding, retained/fresh substitution, hard-link and output aliases, CWD-changing hooks, monotonic deadlines, staged-input clock swaps, wrong fresh-package replacement, forged child projections, recursively closed/bounded schema discovery, no-clobber publication, and gate-after-retention behavior. The existing manufacturing replay also final-rereads every staged input after its last injected clock and captures child output before another clock hook. The macOS/Windows boundary matrix runs the focused v1.476 module once. Real KiCad 10 E2E passed with a manufacturing-capable multilayer board, an exact positive ZIP replay, a retained incomplete gate, schema/source/nonclaim audits, and report/ZIP substitutions rejected without output |
| v1.477 fresh routing/native-DRC/manufacturing handoff | A Python-only composer captures the complete v1.476 source closure, its retained 4 MiB report, and one retained 32 MiB normalized native KiCad DRC report under a 724 MiB direct-input ceiling. It strictly validates both retained bindings, freshly reproduces the v1.476 bytes, then invokes the existing Rust DRC verifier against the same staged routed board, project, and custom rules. One closed path-free 1 MiB report binds every direct source, bounded handoff and DRC projections, eight validation flags, at most one stable gate failure, and one domain-separated digest. Only a ready handoff plus a clean exact DRC replay sets `native_kicad_drc_verified`; routing-incomplete and DRC-rejected evidence is retained before the optional ready gate. Source/tool authenticity, manufacturability, fabrication approval, external submission, and release authority remain false or outside scope | Eleven focused API/CLI regressions cover the exact positive chain, retained routing-incomplete and DRC-rejected decisions, routed-board/package/sidecar cross-binding, retained handoff and child-summary substitution, strict summary types, monotonic clocks, CWD restoration, caller mutation, cross-role aliases, binding forgery, recursive schema closure/bounds, no-clobber schema publication, and gate-after-retention behavior. The macOS/Windows boundary matrix runs the focused v1.477 module immediately after v1.476. The release KiCad E2E creates a fresh clean native DRC report for the same real routed board and retained ZIP, retains incomplete routing without DRC execution, audits exact source/nonclaim/schema fields, and rejects retained DRC or v1.476-report substitution without output. |
| v1.478 policy-pinned routing/DRC fabrication release | A Python release composer captures the complete v1.477 routing/native-DRC/manufacturing closure before any approval-provider hook, requires the exact retained v1.477 bytes, and independently invokes the existing Rust fabrication-authorization verifier with one externally supplied canonical policy digest and the exact manufacturing package retained by the routing chain. The outer 4 MiB, path-free report binds the full routing/DRC result, fabrication assessment, policy identity, signer-sorted submitted approvals, validation flags, stable dual-gate outcome, and a domain-separated digest under a 1,469 MiB source-union ceiling. Release authorization requires both routing/native-DRC manufacturing readiness and fabrication authorization; either valid negative remains inspectable before the optional final gate. Malformed, unpinned, substituted, aliased, oversized, observably changed, or forged child material produces no report. The point-in-time decision does not authenticate tools, sources, policy provenance, factory receipts, manufacturability, capacity, submission, ordering, payment, or one-time challenge use | Eleven focused API/CLI regressions cover positive composition, each independent retained negative, exact policy/package/summary binding, closure-before-approval capture, approval/internal-source hard-link rejection, staged policy mutation, helper approval substitution, backwards clocks, CWD-changing path hooks, renderer/binding/nonclaim forgery, recursive schema closure and bounds, schema publication, and gate-after-retention. The macOS/Windows boundary matrix runs this module immediately after v1.477. Real KiCad 10 E2E passed with a dedicated clean routed board, exact convergence and v1.475-v1.477 replay chain, the same manufacturing ZIP carried into two policy-pinned Ed25519 approvals, an authorized two-signer release, retained routing and quorum negatives, recursive schema/nonclaim audits, and approval and policy-pin tampering rejected without output |
| v1.479 externally digest-pinned release entrypoints | A strict Python consumer leaves the public v1.478 authority and wire format unchanged, adds one canonical retained v1.478 report, and observes the routing pcbex, authorization pcbex, and KiCad CLI only after the complete source/approval closure has been captured. Each command resolves to one absolute executable with no wrapper arguments, is bounded to 128 MiB and recognized as host-native PE/Mach-O/ELF, and must equal a role-specific externally supplied SHA-256 pin before the same resolved entrypoint enters fresh v1.478 replay. Retained and fresh reports must bind the same domain-separated, time-invariant source/routing/approval/scope/pipeline/package/receipt/policy subject; the fresh assessment time and decision are never copied from history. The closed 8 MiB path-free result embeds the fresh v1.478 report, retained raw identity/shared subject digest, three fixed format/size/observed/expected pin records, eight validation flags, one stable nested-negative gate, and a domain-separated binding under a derived 1,857 MiB ceiling. Valid fresh negatives remain inspectable before the final gate; malformed pins, wrappers, non-native/unsafe/aliased/changed entrypoints, subject mismatch, or replay failure produces no outer report. Only digest agreement is promoted; historical-decision reuse, origin/signature, libraries/plugins/resources, loader/OS/environment state, full toolchain provenance, sandboxing, same-principal race exclusion, source/policy/receipt authenticity, manufacturability, submission, capacity, ordering, payment, and one-time use remain false or explicit nonclaims | Twelve focused API/CLI regressions cover positive and retained-negative composition, historical-versus-fresh assessment-time separation, retained raw/subject binding, all three exact role pins, wrong and non-native binaries, wrapper rejection, injected-clock executable mutation and immutable-evidence hardening, private exact/subject retained-v1.478 validation, nested/pin/binding/nonclaim forgery, recursive schema closure/bounds, schema discovery, and gate-after-retention. The macOS/Windows boundary matrix runs this module immediately after v1.478. Real KiCad E2E passed with the release pcbex and installed KiCad entrypoint pins, fresh positive reassessment, retained nested negative, recursive schema/nonclaims, and a wrong-pin hard failure without output |
| v1.480 policy-pinned signed factory-receipt release | A dedicated optional policy role maps 1–100 unique factory identities and providers to role-disjoint non-weak Ed25519 keys under an externally expected canonical policy digest. Rust signs and strictly verifies one exact accepted passing normalized factory receipt, its byte-identical manufacturing package, receipt projection, raw/canonical policy identity, factory identity, attestation ID, 64-hex challenge, and bounded time window. A Python outer boundary captures the full v1.479 closure plus retained report, receipt, policy, and signed attestation under a derived 1,994 MiB ceiling; freshly replays the time-invariant v1.479 subject before and after the digest-pinned authorization verifier; requires the retained receipt-assessment instant to lie inside the exact fabrication-authorization window; cross-binds every package, receipt, policy, signature, and executable identity; and retains a closed 16 MiB authenticated or valid negative snapshot. Invalid signatures, pins, bindings, aliases, mutations, unsafe inputs, child failures, or replay mismatches produce no report. Only configured-key receipt authenticity and the conjunctive outer release decision can become true; legal identity, TLS/raw-response authenticity, trusted time, source/tool/policy provenance, manufacturability, submission, capacity reservation, ordering, payment, and one-time use remain false | Focused Rust tests cover an exact golden signature, positive and inactive-window signing/verification, signature tampering, dedicated policy validation and cross-role separation, closed schemas, and CLI parsing; thirteen focused Python API/CLI tests cover positive composition, inactive and nonoverlapping-window retained evidence, exact submitted-attestation preservation, built-in path-only capture, strict nested gates, two-replay subject substitution, staged and caller-package mutation, nonrollback/final deadlines, CWD restoration, recursively closed and bounded schema discovery, and final-gate retention. The macOS/Windows matrix runs both the Python boundary and Rust cryptographic module immediately after v1.479. Real KiCad 10 E2E passed with a release-binary factory key, positive and expired direct verification, positive and retained-negative composition from the complete v1.479 closure, recursive schema/nonclaim audits, and tampered signatures rejected without output |
| v1.481 local signed factory-receipt release reservation | A Unix-only public agent command requires one canonical retained v1.480 report, freshly replays the same time-invariant signed-release subject from its complete routing/DRC/manufacturing, fabrication-approval, executable-pin, receipt, policy, and attestation closure, and admits only a fresh authenticated result while both the receipt-attestation and fabrication-authorization windows remain active. One closed path-free marker no larger than 16 KiB binds retained/fresh report identities and bindings, package, receipt, policy, signer, verifier, both challenges, both windows, and an externally expected ledger ID. A hidden Rust helper pins an existing effective-UID-owned mode-`0700` local Unix ledger, validates its fixed 4 KiB manifest, keeps every protected replay path outside it, and synchronously installs one deterministic challenge name without replacement. Existing leaves burn the local challenge, including corrupt contents; post-invocation failures are conservatively treated as potentially committed. Global uniqueness, trusted time, source/tool/policy provenance, legal factory identity, capacity, external submission, order, payment, and exactly-once execution remain explicit nonclaims | Seven focused Python regressions cover positive compact binding, changed-subject and fresh-negative refusal, canonical retained bytes, absolute-ledger and bounded helper staging, protected data/entrypoint forwarding, uncertain completion semantics, public-hook working-directory restoration, and CLI fresh-replay-before-commit order. Rust unit and real-helper integration tests cover canonical marker/manifest parsing, active-window checks, recursive schema closure, hidden capability surface, secure ledger mode, input and symlink overlap, durable `0600` publication, repeat and corrupt-marker collision retention, and non-Unix fail-closed behavior. The macOS/Windows boundary matrix runs both focused modules immediately after v1.480; the release KiCad 10 E2E passed with a fresh authenticated release, one durable marker, exact repeat rejection without mutation, expired-evidence refusal, tampered receipt/source hard failures, and recursive schema/nonclaim audits |
| v1.482 durable signed factory-release adapter submission | One Unix-only public submit command consumes the exact v1.481 reservation marker and manufacturing ZIP from the same pinned ledger, derives one reservation-stable domain-separated idempotency key, and durably commits a canonical 16 KiB intent before one no-redirect POST. Changing the separately bound nonce or endpoint collides with that intent instead of minting a second submit key. Existing intent/result pairs replay exact durable evidence; an intent without a result blocks package retransmission. A separate GET reconciliation command sends no ZIP and retains one canonical 32 KiB observation for each reconciliation ID. Responses are capped at 64 KiB, duplicate-free, closed, cross-bound to the exact release/package/request, and rejected on credential reflection. Each receipt binds a local attempt-start instant while keeping trusted time false. Unknown transport or response outcomes remain inspectable before a hard final error. Server-side idempotency, legal identity, transport/raw-response authenticity, capacity, order, payment, and exactly-once execution remain false | Four Rust unit regressions cover the reservation-stable key, exact POST/package/header binding, GET-without-body reconciliation, malformed acknowledgement and credential-reflection reduction, canonical intent identity, receipt cross-binding, impossible response shapes, and closed schema discovery. Three real-binary integration regressions cover public schemas/commands, pending submit plus final-gate retention, nonce/endpoint variation refusal before another POST, exact result replay without a second POST, accepted GET reconciliation plus observation replay, transport-unknown retention, intent-only retransmission refusal, secret exclusion, and non-Unix fail-closed behavior. The macOS/Windows matrix runs the focused integration module immediately after v1.481. Real KiCad E2E passed with the v1.481 marker, exact production ZIP, one pending POST, local replay, one accepted GET without ZIP bytes, reconciliation replay, recursive schemas, the local attempt-start field, and all explicit nonclaims |
| v1.483 policy-pinned authenticated factory-release adapter responses | Two Unix-only commands layer a strict RFC 9421 Ed25519 and RFC 9530 `Content-Digest` profile over the unchanged v1.482 intent, acknowledgement, and receipt bytes. An independently supplied canonical policy digest plus exact policy-source identity selects a role-disjoint key bound to one factory and provider. The signature covers response status, digest, media type, request adapter/schema/profile headers, idempotency key, nonce, optional reconciliation ID, release subject, package, factory, method, and target URI. The local evaluation instant must lie inside a signer-declared positive window no longer than the policy maximum, while trusted time remains false. A closed 64 KiB outer report retains either fully re-verifiable positive signature evidence or one negative failure code with no signer/signature claim. It commits before the compatible v1.482 receipt, allowing crash repair without another request; legacy unsigned observations require a fresh reconciliation ID. TLS, legal identity, key custody, trusted timestamping, server-side idempotency, capacity, order, payment, and exactly-once execution remain false | Five unit regressions cover an exact stable Ed25519 signature vector, positive cryptographic round-trip, body/status/signature-window tampering, missing/duplicate/credential-reflecting headers, closed schemas, positive/negative invariant coupling, and explicit nonclaims. Three real-binary integration regressions independently construct RFC 9421 response signatures, exercise one signed POST plus local replay, one signed GET without ZIP bytes plus observation replay, authentication-first and compatible-record retention, unsigned-response negative retention without a second POST, public schema/command discovery, secret exclusion, and non-Unix fail-closed behavior. The macOS/Windows boundary matrix runs the focused integration module after v1.482, and the real KiCad release path exercises the same signed adapter exchange against an exact production ZIP |
| v1.484 authenticated monotonic factory-release adapter state | A separate v2 RFC 9421 profile preserves every v1.482 and v1.483 command and wire contract while signing three response state headers and the client's accepted sequence/digest head. One domain-separated semantic state binds sequence, predecessor, idempotency key, stable submission/factory/provider/release/package identity, and status without binding re-signing time or reconciliation ID. Genesis must be sequence zero; later responses may only exactly replay the head or advance one linked generation. The verifier separately reports cryptographic authentication and continuity, rejecting rollback, equal-generation equivocation, gaps, forks, submission substitution, and successors after accepted/rejected terminal state. Authenticated observations commit before sequence-keyed state entries and unchanged compatible receipts, so either later record repairs locally without another request. Complete chain loading re-verifies every referenced report under a 10,000-state ceiling, and terminal reconciliation stays local. A pre-commit report explicitly keeps selected-ledger commitment false; the later immutable entry and no-replace winner establish selection. Global non-equivocation, ledger rollback resistance, TLS/legal identity, trusted timestamping, server idempotency, capacity, order, payment, and exactly-once execution remain false | Seven focused unit regressions pin exact state/signature bytes, verify real POST-to-GET pending/accepted progression, exercise exact replay, reject rollback/equivocation/gap/fork/terminal mutation/submission changes, cover request-head/body/state-header/signature substitution, preserve authenticated negative continuity and wrong-state-digest evidence, round-trip canonical reports/entries, audit four recursively closed schemas, and assert explicit nonclaims. The real-binary integration suite independently constructs the v2 signature base, checks request-head delivery and GET-without-ZIP behavior, simulates crash gaps after observation retention and repairs state/receipt locally, advances exactly once to accepted, returns later terminal queries without network, discovers all commands/schemas/capabilities, and scans the ledger for credential retention. The macOS/Windows boundary job parses the new public commands and keeps their durable execution fail-closed off Unix; the real KiCad release path exercises the v2 adapter over the exact production ZIP |
| v1.485 policy-pinned factory-release state transparency | One Unix-only verifier fully reloads the unchanged v1.484 chain before selecting its exact current state entry and observation. A separate canonical policy with an independently supplied semantic SHA-256 pins 1–100 unique role-disjoint non-weak Ed25519 log keys and a bounded local freshness interval without changing the v1.484 organization-policy digest. A domain-separated state projection feeds a custom-domain Merkle leaf; a bounded RFC 6962-shaped audit path must reconstruct one signed tree root, and the tree-head signature binds log ID, size, root, and observation instant. The canonical report binds both policy digests, exact state/observation/receipt identities, state projection, embedded receipt, tree-head digest, evaluation instant, positive verification flags, and explicit nonclaims before one sequence/log-keyed durable no-replace commit. Exact retry returns retained bytes; a conflicting receipt or policy cannot replace the winner. The command consumes supplied evidence and performs no network request. Global non-equivocation, tree-head consistency, selected-ledger rollback resistance, trusted time, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Five focused unit regressions cover odd-sized Merkle trees, exact report replay, state/leaf/path/root/signature/key/policy/time and chain-completeness substitution, canonical/duplicate/oversized receipt rejection, independently pinned standalone policy parsing and role separation, three recursively closed schemas, bounded filenames, and explicit nonclaims. Public CLI/capability tests expose all schemas and the Unix-only verifier, while the macOS/Windows boundary matrix runs the cryptographic module before platform-specific setup. The real KiCad release path builds a separate log key and policy, signs the exact accepted v1.484 head, rejects a forged signature without output, retains and locally replays one immutable report despite a later stale evaluation argument, audits exact policy/receipt/ledger identities and every nonclaim, and scans retained records for credential leakage |
| v1.486 factory-release transparency checkpoint consistency | One Unix-only verifier leaves every v1.484/v1.485 byte unchanged, fully reloads the selected monotonic state chain, and rebinds both embedded inclusion reports to their exact retained state entries, observations, organization-policy pin, transparency-policy pin, signed heads, and receipt bytes. Generation 1 requires an exact retained sequence/log-keyed v1.485 anchor; each later generation names the exact preceding v1.486 source identity. The same release subject, log ID/key, and policy pins must hold while state sequence and signed observation order cannot reverse. A strictly larger tree and a bounded RFC 6962-shaped path must reconstruct both signed roots; smaller trees, same-size equivocation, identical non-extension, path/head substitution, and signature failure are rejected. Canonical reports embed both inclusion reports, exact proof and identities, predecessor, generation, roots, evaluation, positive claims, and explicit nonclaims before deterministic no-replace retention. Exact retry of the latest evidence returns retained bytes; the command performs no network request. Global non-equivocation, selected-ledger rollback resistance, trusted time, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Four focused cryptographic and contract regressions exhaust every strict extension for 1–32 balanced/unbalanced leaves, reject path tampering, rollback, same-size equivocation, time reversal, log/key substitution, and noncanonical/duplicate/oversized proofs, and audit two recursively closed schemas plus bounded filenames. Public CLI/capability tests expose both schemas and the Unix-only verifier. The macOS boundary runs the shared prefix filter while Windows reuses one linked bin harness in a dedicated 45-minute job. The real KiCad 10 release path independently constructs signed 1-, 3-, and 4-leaf views, rejects a modified consistency node without output, bootstraps from the exact v1.485 report, retains and replays generation 1 despite a later stale evaluation argument, extends through generation 2, audits predecessor/report/proof identities and every nonclaim, and scans retained records for credential leakage |
| v1.487 factory-release transparency witness quorum | A cross-platform signer validates one self-contained v1.486 report and externally digest-pinned canonical witness policy before issuing a domain-separated Ed25519 receipt over the exact report identity and complete current signed tree head. The Unix verifier reloads the latest complete v1.484–v1.486 chain, rejects an older or different checkpoint, and admits only fresh receipts from unique configured organization, witness, and non-weak key tuples under a 2–100 organization threshold. Witness keys cannot reuse the log key. The recursively closed report embeds the full consistency report, policy, sorted receipts, exact identities, threshold, freshness extrema, positive claims, and explicit nonclaims before generation/policy-keyed no-replace retention. Exact receipt-set retry returns retained bytes after expiry; a different set conflicts. Global non-equivocation, real organizational independence, selected-ledger rollback resistance, trusted time, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Focused Rust tests cover canonical policy ordering, duplicate organization rejection, semantic digest stability, closed bounded schemas, and durable filename bounds; the shared release-mode cryptographic filter and capability inventory exercise all new public commands on macOS and Windows. Real KiCad 10 E2E derives two independent witness keys, signs one exact generation-2 checkpoint, rejects a below-threshold set and a separately signed same-size split root without output, retains a two-organization quorum, replays it after expiry, rejects an alternate valid receipt set as a durable conflict, audits embedded identities and every claim, recursively checks all three schemas, and scans the ledger for private-key leakage |
| v1.488 factory-release transparency external-log anchoring | One Unix-only verifier leaves every v1.484–v1.487 wire contract unchanged, fully reloads the selected monotonic state, inclusion, consistency, and witness chain, and accepts only the exact latest canonical v1.487 quorum report. A separate externally digest-pinned canonical policy selects 1–100 non-weak Ed25519 external-log keys whose IDs and keys cannot reuse the source log or any witness role. A domain-separated leaf binds the complete report identity and release context; the verifier authenticates a bounded signed tree head before interpreting its RFC 6962-shaped inclusion path, then requires the head to be no older than the policy permits at the local evaluation instant. The closed report embeds the complete quorum report, policy, proof, identities, positive claims, and explicit nonclaims before one context-keyed no-replace commit. Exact proof retry returns retained bytes after expiry; a different valid proof conflicts. External-log append-only consistency, global non-equivocation, selected-ledger rollback resistance, trusted time, real organizational independence, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Five focused Rust regressions cover canonical policy pins and ordering, duplicate and weak keys, signature-before-inclusion ordering, malformed or noncanonical paths, recursively closed bounded schemas, and deterministic bounded filenames. Capability and CI policy tests expose all three schemas plus the public verifier across platforms while durable execution fails closed off Unix. Real KiCad 10 E2E independently builds signed three-leaf and alternate four-leaf views, rejects an unauthenticated root and an authenticated invalid path, retains one valid anchor, replays it after expiry, rejects alternate evidence as a durable conflict, rejects a witness key reused as an external-log key, audits exact identities, claims, schemas, and deterministic storage, and scans the ledger for private-key leakage |
| v1.489 factory-release transparency external-log consistency | One Unix-only verifier leaves every v1.484–v1.488 byte unchanged and reloads the exact retained anchor plus its complete predecessor chain. A canonical proof carries the unchanged external policy/log identity, complete old and new signed heads, their exact digests, and a bounded RFC 6962-shaped consistency path. Both heads authenticate before size, time, or path interpretation; the verifier then requires one log, algorithm, and key, strict tree growth, nonreversing signed time, a locally fresh current head, and one path that reconstructs both roots. Generation 1 starts at the exact retained v1.488 head, each later proof starts at the exact prior v1.489 current head, and a 1–10,000 no-replace chain preserves every accepted transition. Exact latest-proof retry returns retained bytes after expiry while a competing branch conflicts. The claim covers append-only consistency only for the selected external-log views; global non-equivocation, ledger rollback resistance, trusted time, real organizational independence, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Five focused Rust regressions cover balanced and unbalanced extensions, path tampering, rollback, equivocation, key substitution, canonical parsing, recursively closed bounded schemas, and bounded filenames. Capability and CI policy tests expose both schemas and the verifier across platforms while durable execution fails closed off Unix. Real KiCad 10 E2E rejects an unauthenticated current head and a signed invalid path, retains valid 3→4 and 4→5 transitions, rejects a competing 3→4 branch, replays generation 2 byte-for-byte after expiry, audits exact anchor/predecessor/proof identities, claims, schemas, deterministic storage, and scans the ledger for private-key leakage |
| v1.490 factory-release transparency external-log gossip | One Unix-only verifier leaves every v1.484–v1.489 wire byte unchanged, reloads the complete retained chain, and selects the exact latest v1.489 head. A canonical 64 KiB receipt embeds one policy-matched external signed head plus a separately configured observer ID, non-weak Ed25519 key, bounded receive/expiry window, exact head digest, and domain-separated signature. Both external-log heads and the observer authenticate before size, time, or proof interpretation. Identical size/root needs no proof; unequal sizes require one exact bounded v1.489 consistency proof in the older-to-newer direction; equal-size divergent roots fail closed. Observer ID/key reuse across selected log, witness, and factory roles is rejected. One deterministic local-generation/observer-keyed no-replace report embeds the complete evidence under 16 MiB; exact retry returns retained bytes after expiry and competing evidence conflicts. The claim covers only consistency between the selected local and observer views; global non-equivocation, observer quorum, real organizational independence or key custody, ledger rollback resistance, trusted time, TLS/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Five focused Rust regressions cover all three accepted relationships, genuine observer signatures, receipt/head/proof/time tampering, split views, proof omission or redundancy, canonical/duplicate/uppercase rejection, recursively closed bounded schemas, and observer-bound filenames. Capability and CI policy tests expose both schemas and the verifier across platforms while durable execution fails closed off Unix. Real KiCad 10 E2E independently signs a 6-leaf observer view over the retained 5-leaf head, rejects a tampered receipt, equal-size split root, missing proof, observer role collision, and competing receipt without output, retains the valid comparison, replays byte-for-byte after expiry, audits exact local report/policy/receipt/proof identities and deterministic storage, recursively checks schemas, and scans the ledger for both external-log and observer private-key leakage |
| v1.491 factory-release transparency external-log gossip quorum | One cross-platform acquisition command sends a canonical request through a 1–600 second no-redirect HTTPS boundary, accepts only HTTP 200 JSON up to 1 MiB, and retains create-new canonical observation plus secret-free hash-bound transport evidence. A digest-pinned static policy admits 2–100 distinct organization/observer/non-weak-key tuples with a bounded receipt-age window. The Unix verifier reloads the complete v1.484–v1.489 chain, binds every transport receipt to the exact request, response, local head, policy, and observer tuple, then independently replays the unchanged v1.490 signature, role, freshness, and Merkle checks. Duplicate identities or evidence fail, and all selected observers must agree on one exact signed external head before their organizations count. Below-threshold evidence reaches only the requested output; a met quorum receives one local-generation/policy-keyed 32 MiB no-replace report. Concurrent exact writers converge, reordered exact retries survive expiry, and alternate evidence conflicts. Global non-equivocation, real organizational independence or key custody, selected-ledger rollback resistance, trusted time, endpoint/legal identity, server idempotency, capacity, order, payment, and exactly-once execution remain false | Three focused Rust regressions cover canonical policy order and digest stability, duplicate organizations and weak keys, closed bounded schemas, endpoint and credential-name rules, same-head quorum, valid later forks sharing one local prefix, and same-size split-view rejection. Capability and CI policy tests expose four schemas and both commands across platforms while durable execution fails closed off Unix. Real KiCad 10 E2E uses an authenticated loopback HTTP adapter to acquire two exact-head observations and one separately signed fork; rejects status, MIME, oversize, redirect, and timeout failures without output; verifies Bearer/request/response bindings and secret exclusion; retains but does not commit a below-threshold report; rejects forked agreement and tampered or stale evidence; converges concurrent identical commits; replays reordered inputs byte-for-byte after expiry; rejects alternate durable evidence; and audits claims, deterministic storage, and every recursive schema |
| v1.492 factory-release external-gossip observer key rotation | Every generation-zero trust state binds one immutable v1.491 base-policy digest, policy ID, organization, observer, and initial non-weak Ed25519 key. A successor advances exactly one of 4,096 bounded generations only when retained and successor keys sign the same domain-separated payload carrying both generations, the preceding transition digest, both keys, and nondecreasing explicit time. The Unix apply path reloads complete history, rejects historical-key reuse, and publishes one context-hashed generation record through descriptor-pinned no-replace retention; identical concurrent writers converge while fork, gap, mutation, and stale replay fail. Complete latest histories derive one canonical unchanged-v1.491 effective policy and semantic pin. A 64 MiB self-contained report requires the exact effective policy inside an exact durable v1.491 quorum, embeds every transition and current state, and rejects an older policy after selected rotation. Host-ledger rollback resistance, trusted time, global non-equivocation, real organization independence, legal identity, capacity, order, payment, and exactly-once execution remain false | Three core Rust regressions cover dual-signed two-step chains, policy/identity/digest binding, wrong old key, same key, initial and historical key reuse, time reversal, replay, fork, canonical parsing, bounded filenames, and recursively closed schemas. Two real-binary ledger regressions cover concurrent exact apply, exact retry, competing fork, signature mutation, stale replay, two retained generations, effective-policy derivation, secret exclusion, and all three schemas. Capability and CI policy tests expose three schemas and five commands while Unix-only mutations fail closed elsewhere. Real KiCad 10 E2E rotates one live observer, reacquires two effective-policy-bound remote observations, retains a fresh v1.491 quorum, converges concurrent trust-report writers, replays exact bytes, rejects the old policy, and audits every embedded identity, claim, nonclaim, schema, ledger record, and secret boundary |
| v1.493 governed factory-release external-gossip organization registry | One independently pinned empty generation-zero artifact binds the immutable v1.491 base-policy digest, policy ID, registry ID, and one non-weak Ed25519 authority key that must remain role-disjoint from every initial, historical, and current observer key. Each authority-signed transition advances exactly one of 4,096 bounded generations, names the preceding transition digest, carries a nondecreasing explicit time and operator-controlled reason digest, and either admits one exact latest v1.492 observer trust-state digest, suspends one active organization, or permanently revokes one admitted organization. Unix export/apply reload complete registry and observer histories around descriptor-pinned no-replace publication; identical concurrent writers converge while fork, gap, stale trust, signature mutation, role reuse, and stale replay fail. The 128 MiB self-contained report requires the exact selected-ledger v1.492 trust report and latest rotations, embeds genesis plus every transition and current registry, and admits a selected quorum member only when its organization remains active and its exact current trust digest is admitted. Unselected suspended organizations do not invalidate other selected members. Authority threshold governance/key rotation, host-ledger rollback resistance, trusted time, global non-equivocation, real organization independence, legal identity, capacity, order, payment, and exactly-once execution remain false | Three core Rust regressions cover admission, suspension, permanent revocation, authority/policy/trust binding, wrong key, signature mutation, transition chaining, canonical parsing, filenames, report replay, and recursively closed bounded schemas. Two real-binary ledger regressions cover genesis initialization and pinning, export, concurrent exact apply, retry, wrong authority, signature tampering, observer rotation followed by stale-admission rejection, fork conflict, suspension/revocation, deterministic records, secret exclusion, and all three schemas. Capability and CI policy tests expose three schemas and five commands while durable mutations fail closed off Unix. Real KiCad 10 E2E admits the rotated and unrotated live observers, converges concurrent registry-report writers, replays exact bytes, rejects the same durable quorum immediately after organization suspension, and audits every embedded artifact, transition, claim, nonclaim, schema, deterministic ledger record, and authority-secret boundary |
| v1.494 factory-release external-gossip registry authority rotation | Every v1.493 wire artifact remains unchanged. A separate 16 KiB authority-rotation event advances the shared registry chain by exactly one generation, binds the immutable policy and registry context plus the prior transition-or-rotation digest and nondecreasing explicit time, and requires old/new Ed25519 signatures over one domain-separated payload. Applying it preserves every organization and admission, rejects any historical authority-key reuse, and keeps the complete authority history disjoint from every initial, rotated, and current observer key. Unix transition and rotation mutations coordinate on the descriptor-pinned ledger manifest before no-replace publication so one heterogeneous event wins each generation and identical retries converge. The new 128 MiB report embeds typed mixed history and the exact v1.492 trust report; the legacy v1.493 verifier rejects rotations. Threshold governance, host-ledger rollback resistance, trusted time, global non-equivocation, real organizational independence, legal identity, capacity, order, payment, and exactly-once execution remain false | Four core registry regressions now cover dual signatures, successor possession, empty and populated membership preservation, old-key rejection, signature mutation, time/digest/generation invariants, canonical mixed-event parsing, and five recursively closed bounded schemas. Three real-binary registry regressions cover wrong-current-key signing, tampered successor signature, concurrent exact rotation, exact retry, post-rotation signing, heterogeneous transition/rotation races, historical authority-key reuse, observer-role collision, deterministic records, and private-key exclusion. Capability and CI policy tests expose two new schemas and three new commands while durable execution stays Unix-only. Real KiCad 10 E2E builds a fresh two-organization registry, rotates its authority, retains and replays the mixed-history quorum report, proves the v1.493 verifier fails closed, audits every event identity, positive claim, explicit nonclaim, schema, record, and secret boundary |
| v1.495 factory-release external-gossip registry threshold governance | Every v1.493 and v1.494 wire artifact remains unchanged. One 32 KiB retained-root-signed governance document binds the immutable base-policy and registry context, exact activation generation and semantic state digest, current root, 2–100 ordered distinct authority IDs and non-weak Ed25519 keys, a threshold of at least two, and issue time. Each 128 KiB threshold transition embeds that document and advances the shared generation/digest/time chain only with enough ordered, distinct, governance-matched signatures over one admission, suspension, or revocation. The first accepted transition pins `active_governance_sha256`; root-only transitions and root rotations then fail closed. Unix application serializes all three event types on the same descriptor-pinned manifest, rechecks latest observer trust, rejects root/governance/observer role reuse, converges exact retries, and retains one no-replace event. The 128 MiB report replays legacy transitions, root rotations, and threshold events from pinned genesis plus the exact latest v1.492 trust report; v1.493 and v1.494 verifiers reject governed history. Independent control of configured keys, governance rotation, host-ledger rollback resistance, trusted time, global non-equivocation, legal identity, capacity, order, payment, and exactly-once execution remain false | Core Rust regressions cover canonical governance ordering, duplicate identities and keys, root signature and activation-state binding, insufficient or substituted approvals, signature mutation, continued threshold operation, root-only lockout, stale governance rejection, closed bounded schemas, and deterministic filenames. Real-binary registry tests cover public/private key pairing, role collision, concurrent exact threshold apply, exact retry, private-key exclusion, legacy command lockout, export, and all eight registry schemas. Capability and CI policy tests expose three schemas and four commands while durable execution stays Unix-only. Real KiCad 10 E2E builds a legacy-transition/root-rotation/threshold-transition history, retains and replays the threshold-aware quorum report, proves the v1.494 verifier and root-only signer fail closed, and audits embedded governance, approvals, identities, claims, nonclaims, schemas, deterministic records, and every private-key boundary |
| v1.496 factory-release external-gossip registry governance rotation | Every v1.493–v1.495 artifact remains byte-compatible. One root-signed successor governance binds the exact active registry generation and semantic state digest while changing at least one threshold, identity, or authority key. A 256 KiB self-contained rotation advances the shared registry generation/digest/time chain only after ordered distinct approvals satisfy both the retained and successor thresholds over one domain-separated payload. Applying it preserves the registry root, organizations, and observer admissions, atomically retains the successor digest, and makes the old governance unusable for later transitions. The selected-ledger loader rejects cross-type generation conflicts, gaps, stale replay, signature substitution, and observer-role collisions across four event types. A 128 MiB report replays every root and governance artifact from pinned genesis; the v1.495 verifier rejects rotation history. Overlapping quorums do not prove independent governance control, and governed root rotation, host-ledger rollback resistance, trusted time, global non-equivocation, legal identity, capacity, order, payment, and exactly-once execution remain false | Core Rust regressions cover current-state successor binding, semantic no-op rejection, old/new threshold shortfalls, canonical approval ordering, both signature sets, stale apply, old-governance invalidation, successor operation, mixed-event parsing, fail-closed v1.495 parsing, and ten closed bounded schemas. Real-binary registry tests change 2-of-3 governance to 3-of-3, reject insufficient and tampered old approval sets, retain and exactly replay one rotation, reject the old governance, apply a successor-governed revocation, audit deterministic records, and exclude every private key. The CLI exposes two new schemas and four new commands while durable mutation and report retention stay Unix-only. The real KiCad 10 E2E extends the retained legacy/root/threshold history with a dual-quorum rotation, replays the new self-contained report, proves the v1.495 verifier fails closed, and audits both governance documents, approval sets, explicit nonclaims, schemas, no-replace records, and secret boundaries |
| v1.497 governed factory-release external-gossip registry-root rotation | Every v1.493–v1.496 artifact remains byte-compatible. A distinct prospective root signs successor governance bound to the exact active registry generation and semantic state digest. One 256 KiB self-contained event binds old/new roots, old/new governance digests, the exact next generation, prior event digest, and nondecreasing time; retained and successor policies must each supply ordered distinct policy-matched approvals satisfying their own threshold over the same domain-separated payload. Applying the event atomically replaces `authority_public_key` and `active_governance_sha256` while preserving organizations and observer admissions. The selected-ledger loader serializes five event types, rejects cross-type conflict, gap, stale replay, historical root reuse, signature substitution, and observer/governance/root role collision. A 128 MiB report replays the complete history from pinned genesis; the v1.496 verifier rejects the fifth event. Prospective-root possession and dual quorum satisfaction do not prove independent governance control, host-ledger rollback resistance, trusted time, global non-equivocation, legal identity, capacity, order, payment, or exactly-once execution | Core Rust regressions cover same-root rejection, exact-state successor binding, both quorum shortfalls, canonical approval ordering, old/new signature mutation, atomic root/governance replacement, organization preservation, old-governance invalidation, successor operation, mixed-event parsing, fail-closed v1.496 parsing, and closed bounded schemas. Real-binary registry tests extend a governance-rotation history with the governed root event, reject insufficient and tampered approval sets, retain and exactly replay it, continue a threshold transition under the successor root, audit the fifth deterministic record, and exclude every private key. The CLI exposes two schemas and four commands; durable mutation and report retention remain Unix-only. The real KiCad 10 E2E creates independent successor root and governance keys, retains and replays the five-event report, proves the v1.496 verifier fails closed, rejects pre-rotation governance, accepts successor-root governance, and audits event identities, claims, explicit nonclaims, schemas, no-replace records, and secret boundaries |
| v1.498 portable factory-release external-gossip registry history audit | Every v1.493–v1.497 wire artifact remains byte-compatible. One 128 MiB canonical history starts at the exact empty generation-zero registry, records exact byte length and SHA-256 for genesis and each event, and carries at most 4,096 typed organization transitions, root rotations, threshold transitions, governance rotations, or governed root rotations. Replay calls the production apply verifier for every event, enforces contiguous generation/digest/time state, historical-root uniqueness, and root/governance role separation, and emits one entry per generation plus the final registry computed only from the supplied evidence. Unix export pins and revalidates the selected ledger, base policy, and genesis and requires replay to equal the selected final state. Cross-platform audit atomically publishes its report and computed registry as an alias-free no-clobber set. A passing audit does not prove the supplied generation is the selected latest head, host-ledger rollback resistance, global non-equivocation, trusted time, independent control, legal identity, capacity, order, payment, or exactly-once execution | Core Rust regression covers exact render/parse identity and all five production apply paths while rejecting reorder, replay, omission, non-genesis starts, wrong exact identities, signature mutation, historical root reuse, and inconsistent audit state. Real-binary registry E2E builds and exports a complete five-kind selected-ledger history, independently audits it, validates byte-stable normalization and final-state equality, rejects chain changes and signature tampering without partial outputs, and proves no-clobber export. Two recursively closed bounded schemas and six CLI commands appear in capabilities; export stays Unix-only while validation and audit are cross-platform. Real KiCad 10 E2E exports the existing five-generation release registry, replays it into an atomic audit/final pair, checks exact and semantic identities, validates both schemas, and matches the selected generation-five registry byte for byte |
| v1.499 witnessed factory-release external-gossip registry history checkpoints | Every v1.493–v1.498 wire artifact remains unchanged. One 32 KiB domain-separated Ed25519 checkpoint freshly replays the portable history and binds its exact audit digest, computed final registry digest, generation, final event, active governance, retained final root, and issue time. A 64 KiB self-contained trust state accepts it only within 24 hours and, when a baseline is supplied, rejects rollback, same-generation equivocation, time reversal, or a later audit that does not preserve the exact accepted event/state/root/governance tuple. Exact retry returns the retained state. Independent 32 KiB witnesses sign one exact checkpoint; the 128 KiB quorum report requires 2–100 fresh distinct configured identities and non-weak keys, canonicalizes members, and rejects every historical root or embedded governance key reuse. Valid below-threshold evidence is retained before the optional fail gate. Protected baseline storage, global non-equivocation, trusted time, independent people/organizations, secure key custody, legal identity, capacity, order, payment, and exactly-once execution remain false | Core Rust regressions cover canonical checkpoint/trust/witness/report round trips, retained-root possession, self-signature authentication, idempotent acceptance, rollback, same-generation equivocation, nonextending forks, root rotation, freshness, role separation, distinct witness quorum, and below-threshold evidence. Real-binary registry E2E signs the complete five-event head only with its governed successor root, validates and idempotently reaccepts trust, rejects a governance key as a witness, verifies an order-independent two-witness quorum, and retains a failed one-witness gate report. Four recursively closed schemas, four validators, and four cross-platform operational commands appear in capabilities and CI. Real KiCad 10 E2E reuses the selected five-generation registry history to exercise the same checkpoint, trust, witness, quorum, schema, digest, role-separation, and failure-retention boundaries |
| v1.500 factory-release registry-history witness key rotation | Every v1.493–v1.499 wire artifact remains unchanged. A canonical 32 KiB generation-zero trust state pins one stable witness identity and non-weak Ed25519 key. Each canonical 32 KiB successor rotation advances exactly one of at most 4,096 generations and binds the prior rotation digest, old and new distinct keys, and nondecreasing explicit time under a registry-history-specific domain signed by both keys. Apply authenticates both signatures before atomically publishing the successor state and exported current key; standalone export derives the same key. The unchanged quorum accepts either direct v1.499 identity/key pairs or trust-state files, rejects mixed modes and stale-state key substitution, and reuses complete-history verification so every current witness key remains disjoint from all historical roots and embedded governance authorities. Protected trust-state storage, global non-equivocation, trusted time, independent people or key custody, legal identity, capacity, order, payment, and exactly-once execution remain false | Core Rust regressions cover canonical trust/rotation round trips, wrong retained key, same-key rotation, chained digest continuity, both signatures, replay, generation gaps and exhaustion, missing predecessors, time reversal, stale trust, and closed schemas. Real-binary registry E2E initializes two identities, rotates one key, validates and exports current trust, rejects replay without partial outputs, verifies a mixed-generation trust-state quorum, and rejects the initial key as substitution. Two new recursively closed schemas, two validators, and four operational commands appear in capabilities and cross-platform CI. Real KiCad 10 E2E advances one witness through the same dual-signed chain, checks byte-stable normalization, atomic key export, stale-trust rejection, exact quorum membership, all six checkpoint schemas, and every private-key boundary |
| v1.501 remote factory-release registry-history checkpoint witnesses | Every v1.493–v1.500 artifact and the existing quorum interface remain unchanged. One explicit adapter posts only the accepted checkpoint trust state to HTTPS with redirects disabled, a 1 MiB response ceiling, a 1–600 second shared timeout, optional environment-sourced Bearer authentication, and a hidden loopback-only HTTP test escape hatch. Before the request it replays the exact canonical complete history and reconstructs the trust state; after the response it requires the unchanged 32 KiB canonical witness and reuses complete-history verification for checkpoint binding, freshness, direct-key or current trust-state authentication, and separation from every historical root/governance key. Exact inputs are revalidated before the witness and a 64 KiB receipt are retained together. The receipt binds exact history, checkpoint trust state, request, response, normalized witness, key mode, endpoint, response size, and time. Protected local state, trusted time, endpoint legal identity, operator independence, global non-equivocation, external receipt signing/publication, capacity, order, payment, and exactly-once execution remain false | Module regressions cover closed receipts, HTTPS/loopback policy, URL credential/query rejection, environment-name validation, and canonical parsing. Real HTTP-stack binary E2E covers direct and rotated trust-state keys, Bearer authentication, exact protocol/checkpoint transmission, non-canonical response rejection, identity substitution, atomic no-output failure, receipt normalization and hash bindings, and unchanged local/remote quorum composition. Capability and CI policy tests expose one schema, one validator, and one request command cross-platform. Real KiCad 10 E2E serves canonical direct and rotated witnesses for the existing five-event history, validates exact receipts, and uses both remote outputs in the unchanged direct/trust-state quorum paths |
| v1.502 factory-release registry witness receipt transparency | Every v1.493–v1.501 registry, checkpoint, witness, trust, remote-request, receipt, and quorum artifact remains unchanged. One additive approval-event kind accepts only the canonical closed 64 KiB receipt with a true verification decision, valid transport and digest bounds, a non-weak witness key, and a complete direct-key or trust-generation binding. Its normalized event binds the canonical compact receipt digest, exact checkpoint digest, request digest, response digest, and witness identity before the existing monotonic hash chain, signed checkpoints, anchors, consistency proofs, gossip, witness rotation, and witness quorum apply. Append remains structural admission by the log signer: it does not replay the complete history, retained checkpoint trust, response bytes, or witness signature. Protected log storage, trusted time, endpoint or receipt authenticity, independent operation, global publication/non-equivocation, legal identity, capacity, order, payment, and exactly-once execution remain false | The real HTTP-stack registry E2E appends a rotated-trust-generation receipt, checks every normalized event binding, signs and verifies the exact log checkpoint, and rejects a false verification decision without output. MCP schema regression exposes the additive artifact kind through the unchanged append tool. Real KiCad 10 E2E performs the same receipt append, signature verification, exact digest mapping, and fail-closed negative path while preserving all prior quorum paths; the existing Action approval-log controls accept the same event without new inputs |
| v1.503 verifier-bound factory-release registry receipt admission | Every v1.493–v1.502 artifact and the structural append path remain unchanged. One dedicated command captures an approval log, canonical receipt, complete registry history, retained checkpoint trust state, exact signed-witness response bytes, and exactly one direct key or generation-chained witness trust state. It replays the production five-event history audit, reconstructs checkpoint acceptance and request bytes, matches raw history/trust/request/response/witness digests and response length, requires the receipt's exact trust mode and generation, and re-verifies witness identity, key, freshness, historical root/governance role disjointness, and Ed25519 signature before appending the unchanged v1.502 event. All six inputs are re-read by byte count and SHA-256 before one alias-free no-clobber snapshot is published. This sequential check is not an atomic same-principal snapshot, and multi-receipt admission quorum, protected local state, trusted time, endpoint/legal identity, independent operation, global publication/non-equivocation, capacity, order, payment, and exactly-once execution remain false | Real HTTP-stack registry E2E admits both a direct-key receipt and a rotated-trust-generation receipt, verifies the exact normalized event and signed log head, and rejects a false decision, truncated complete history, substituted response, stale trust generation, and stale witness without output. The capability inventory exposes the dedicated command, while one task-forbidden MCP tool brings the inventory to 154 and enforces the same mutually exclusive trust modes. Real KiCad 10 E2E uses both direct and rotated admission modes, preserves every v1.501/v1.502 artifact, signs and verifies the admitted head, and rejects response substitution and stale admission without partial output |
| v1.504 verifier-bound factory-release registry receipt admission quorum | Every v1.493–v1.503 wire artifact, structural append, and single-receipt admission path remain unchanged. One command captures a log, exact complete history and checkpoint trust state, 1–100 canonical receipt/response pairs, and either paired direct identity/key files or generation-bound witness trust states. It reconstructs the history/checkpoint/request context once, independently matches every raw receipt/response/trust binding, and invokes the production witness-quorum verifier once for Ed25519 signatures, freshness, role separation, and distinct identities/keys. The report additionally requires distinct normalized receipt, response, and witness digests, sorts members and appended events by witness ID, enforces a 2–100 threshold, and binds the exact resulting log ID, entry count, head, and complete digest. All inputs are re-read by bytes/SHA-256 before the alias-free no-clobber log/report set is published. Protected local state, trusted time, atomic same-principal snapshots, organizational independence, endpoint/legal identity, global publication/non-equivocation, ordering, payment, and exactly-once execution remain false | Real HTTP-stack registry E2E admits out-of-order direct-key and rotated trust-state 2-of-2 receipt sets into identical canonical member ordering, validates the exact-log-bound closed 128 KiB report, and rejects a 2-of-3 threshold without either output. The capability inventory adds the schema, validator, and command; one task-forbidden destructive MCP tool brings the inventory to 155. Real KiCad 10 E2E acquires direct and trust-bound receipts for both witnesses, admits both quorum modes, checks canonical member/event order and log binding, and rejects below-threshold admission without partial output |
| KiCad schematic writer MCP/Action parity | The writer is available synchronously and through optional MCP Tasks with a 64 MiB identity-checked retained-file boundary and metadata-only response; the opt-in root Action retains immutable ERC evidence, emits one fixed artifact path, and exposes verified bytes/SHA-256 without replacing `schematic` or changing pipeline inputs | Closed MCP schemas, annotations, dispatch, stale-output, digest-summary, Tasks, stdio, and capability regressions plus Action disabled/success/ERC-rejection/writer-failure/oversize/symlink/stale-root tests cover the 147-tool MCP inventory and 268-input/122-output Action without adding network access |
| Bounded supplier inventory snapshot ingestion | One explicit no-redirect HTTPS GET acquires only the closed catalog-snapshot v1 contract under a shared deadline and 4 MiB ceiling, reads optional Bearer credentials from a named environment variable without retention, reuses the authoritative snapshot/TTL validator, and atomically publishes normalized local evidence plus a secret-free response/snapshot/catalog digest receipt; selection and deterministic pipelines remain offline | Real loopback HTTP-stack, malformed/duplicate/non-finite JSON, response/status/type/timeout bounds, token non-reflection, endpoint policy, output collision/link/race, normalized replay, schema, and receipt/output tamper regressions |
| DRC repair planning | KiCad 10 report normalization and rule-to-repair mapping | DRC normalization tests |
| Automatic DRC repair loop | Bounded candidate generation, KiCad revalidation, convergence guard, atomic clean-output promotion | Injected three-iteration repair and repeated-candidate tests; real KiCad clean-board run |
| Polygon board geometry | Concave/convex outlines, exact polygon keepouts, edge-aware routing/checking/SVG, KiCad line-outline import | L-board routing, triangular keepout bounding-box escape, concavity tests; five-sided example passes real KiCad DRC |
| Exact circular pad geometry | Circle-aware rasterization, checking, SVG, and KiCad import without bounding-box blockage | Circle-corner routing and clearance regressions; KiCad circle-pad import test |
| Exact oval pad geometry | Rotation-aware capsule rasterization, checking, SVG, and KiCad import | Capsule bounding-box escape and collision regressions; rotated KiCad oval test |
| Exact rotated rectangular pads | Four-corner polygon rasterization, checking, SVG, and KiCad import | 30-degree coordinate regression and 35-degree real-KiCad E2E fixture |
| Curved board outlines | Three-point `gr_arc` and four-point cubic `gr_curve` sampling with a 0.01 mm maximum chord deviation | Semicircle and cubic-curve import regressions plus curved-board real-KiCad E2E fixture |
| Board cutouts and multiple contours | Largest Edge.Cuts contour is the outer outline; enclosed contours are clearance-aware holes | Core routing/checking regression, importer classification test, and holed curved-board E2E fixture |
| Property and fuzz testing | Geometry symmetry/translation properties plus KiCad parser, migrated board-model, length-tuning, and BGA-escape libFuzzer targets | Property tests run on every PR; scheduled/manual fuzz workflow runs all four targets |
| Schema compatibility gate | Draft 2020-12 schema has closed definitions for routing rules, net classes, length/escape/return-path constraints, and stackup; v1 migration is compared semantically with native v2 parsing | Migration regression rejects future versions/unknown fields and verifies every advanced constraint definition is closed |
| Routing performance suite | Criterion scenarios for obstacle, 5/10-net, and board-cutout routing | Benchmark targets compile on every PR and retain local Criterion baselines |
| Routing quality gates | Stable per-net, board-total, and differential-pair metrics with JSON/SARIF output, thresholds, and baseline regression comparison | Geometry-count regression plus CLI JSON/baseline smoke test |
| KiCad analysis artifact bundle | Direct `.kicad_pcb` import emits normalized board, SVG, quality, DRC/DFM, SARIF, Markdown, and a SHA-256-addressed run manifest | CLI parsing, SHA-256, summary regressions, and practical-board command smoke test |
| Analysis bundle comparison | Signed quality deltas plus identity-based new/resolved violations emit JSON, Markdown, SARIF, and input-addressed provenance before an optional regression gate | Core delta/SARIF regressions and end-to-end unchanged/regressed bundle CLI tests |
| GitHub Actions hardware CI | Versioned composite action emits Job Summary, artifact bundles, SARIF, and post-upload violation/regression gates without privileged PR execution | CI exercises the public action contract, unchanged baseline comparison, outputs, and artifact upload |
| GitHub Actions supply-chain pinning | Every external workflow and composite-action dependency is fixed to a reviewed 40-character commit SHA, weekly Dependabot updates retain an auditable version comment, repository enforcement rejects mutable references, and the release audit fails closed if enforcement is disabled | Static tests inspect repository workflows and composite actions, including the public root action, and reject tags, branches, short SHAs, expressions, malformed references, and Docker `uses:` references; release-audit tests cover strict Actions-permissions API schemas and disabled or missing enforcement |
| Versioned fabrication profiles | Immutable JLCPCB/PCBWay standard 2-layer profiles retain aliases, revisions, verification dates, official sources, exact DFM rules, and non-lowering routing corrections in the run manifest | Profile resolution/application unit tests plus KiCad bundle/listing integration and public Action smoke test |
| Distributable DFM profiles | Strict schema-versioned organization profiles apply across CLI, Action, and MCP paths while rejecting malformed, ambiguous, or built-in-colliding identities; analysis binds the source digest | Closed-schema validation unit tests plus normalized-profile and SHA-256-addressed KiCad bundle integration |
| Bounded external DFM profile ingestion | Every external profile selected by validation, DFM checking, KiCad analysis, routing, candidate routing, MCP, or the root Action crosses one 4 MiB stable regular-file boundary; direct and ancestor symbolic links, empty or oversized files, observed mutation, and duplicate JSON object keys fail before the existing closed semantic validator. Built-in profiles, the DFM object embedded in a policy pack, and the public wire schemas remain unchanged; the policy-pack object intentionally stays on its own parser/size contract | Core parser and shared CLI resolver regressions cover the exact size boundary, overflow, empty input, duplicate keys, non-regular files, and direct/ancestor links; pipeline compiler/runner tests cover the 4 MiB runtime descriptor preflight while preserving the public v1 `analysis_descriptor` 64 MiB shape and neighboring analysis limits, and the existing CLI/MCP/Action forwarding suites remain green |
| Cross-phase DFM manufacturing binding | External and built-in DFM selections receive a domain-separated canonical identity plus explicit external raw-source or built-in origin, are applied before `fabricate` DRC/export, and are carried by closed schema-v3 manufacturing manifests; factory repair rejects complete-binding add/drop/substitution and pipeline verification requires exact analysis-to-package equality while policy-pack DFM remains analysis-only | Binding raw-vs-canonical/origin tests, fabricate CLI conflict tests, schema-v3 writer/validator and v1/v2 compatibility regressions, factory repair identity tests, and pipeline accepted/mismatch tests |
| Review-provider command adapter | Provider-neutral schematic review runs without a shell under timeout and streaming output limits, validates before atomic writes, refuses overwrites, and emits a closed hash-bound receipt | Injected provider success, oversized-output rejection, closed-schema, and exact CLI argument-boundary tests |
| Managed AI provider adapters | OpenAI Responses, Anthropic Messages, and Gemini generateContent structured outputs normalize into the closed schematic-review contract over bounded no-redirect HTTPS; credentials are read only from named environments, provider self-identification is replaced with trusted CLI identity, and raw exchanges are retained only as byte length and SHA-256 | Real HTTP-stack loopback tests cover all three provider envelopes and authentication headers, unsafe endpoint rejection, incomplete and oversized responses, absent credentials, closed secret-free receipts, atomic failure, and the composite Action secret boundary |
| Organization policy packs | A closed versioned organization contract combines one DFM profile, electrical policy, explicit AI requirements, simulation-evidence policy, and trusted approval signer keys; all physical and schematic entry points reject conflicting overrides | Strict parsing/collision tests, normalized schema output, analysis provenance integration, Action smoke coverage, and full prepare/sign/pack-trusted verification CLI flow |
| Signed policy-pack distribution | A domain-separated Ed25519 envelope authenticates the normalized pack digest, ID, revision, and signer before extraction; signing keys and outputs refuse overwrite, and the Action applies only a successfully verified pack when a trusted key is configured | Closed envelope schema plus CLI E2E for key generation, signing, extraction, exact content, wrong-key rejection, tamper rejection, and overwrite refusal |
| Monotonic policy trust state | A closed immutable acceptance record pins pack ID, highest revision, exact digest, signer, and signing key; verification permits identical replay or a higher revision while rejecting rollback, same-revision equivocation, and identity/key substitution before CI analysis | Unit and CLI E2E cover initialization, monotonic advance, replay, rollback, conflicting digest, signer change, strict state parsing, output separation, Action propagation, and MCP arguments |
| Append-only policy suspension lifecycle | Complete signed suspension and remediation states are retained under a sequence-, time-, identity-, and digest-bound hash chain; derived records expose pending, released, superseded, and continued decisions at every historical generation without trusting copied counters | Rust regressions reject embedded-evidence mutation, truncation, reordering, duplicate release, forged snapshots, and deployment without remediation; closed ledger/snapshot schemas, CLI capability discovery, MCP tools, and Action inputs/outputs expose the same contract |
| Lifecycle witness key rotation | Immutable trust snapshots bind each long-lived lifecycle-checkpoint observer identity to a current Ed25519 key, exact generation, prior rotation digest, and monotonic rotation time; every one-generation transition requires old-key authorization and new-key possession under a lifecycle-witness-specific domain | Rust and CLI E2E cover two-step chained rotation, replay, fork, key substitution, same-key, wrong-old-key, time reversal, identity-bound verification, validated key export, closed schemas, atomic outputs, MCP initialize/sign/apply/export tools, local/remote Action trust-state inputs, and remote receipts binding the accepted trust-state digest and generation |
| Lifecycle public-log anchoring | Domain-separated lifecycle-checkpoint leaves use RFC 6962 tree splitting and bounded audit paths; a separately trusted Ed25519 tree-head signature binds log identity, exact tree size, root digest, and an observation time that cannot predate the checkpoint | Unit and CLI E2E verify odd-sized inclusion and reject checkpoint, sibling, index, root, signature, trust-key, trusted-log identity, malformed-proof, and time substitution; closed schemas, MCP create/verify tools, and Action inputs, outputs, and fail-closed gate expose the same contract |
| Lifecycle public-log consistency and gossip | Signed retained/current heads require a logarithmic prefix proof across different sizes, while independent observers domain-separate and re-sign exact trusted tree heads in time-bounded receipts | Rust and CLI E2E cover same-tree, both prefix directions, rollback, equal-size split views, missing/redundant/wrong-direction proofs, expired/not-yet-valid receipts, signature/key/identity substitution, closed schemas, MCP tools, and Action fail-closed evidence |
| Remote lifecycle public-log gossip quorum | Closed observation envelopes keep each receipt paired with its optional consistency proof; bounded HTTPS acquisition immediately verifies log and observer signatures before atomic retention, while quorum requires fresh consistent receipts from distinct trusted organizations, observer identities, keys, and receipt digests | Unit and real HTTP-stack CLI E2E cover mixed same/prefix views, canonical member ordering, below-threshold evidence, duplicate organization/observer rejection, 1 MiB/no-redirect/HTTPS transport policy, hash-bound receipts, closed schemas, 69 MCP tools, and 184-input/76-output Action composition |
| Lifecycle gossip observer key rotation | Immutable trust snapshots bind organization, observer identity, current Ed25519 key, exact generation, prior rotation digest, and monotonic rotation time; every one-generation transition requires old-key authorization and new-key possession under a gossip-observer-specific domain | Rust and CLI E2E cover chained rotation, replay, fork, organization substitution, wrong-old-key, same-key, time reversal, atomic key export, trust-bound quorum digests/generations, remote transport binding, closed schemas, 73 MCP tools, and direct/trust-state Action modes |
| Lifecycle gossip organization trust registry | A separately trusted Ed25519 authority advances an ordered organization registry through exact generation- and digest-chained observer admissions, immediate suspensions, and permanent revocations; each admission pins the complete observer trust-state digest and every registry-bound quorum pins the registry identity, generation, and digest | Rust and CLI E2E reject replay, fork, skipped generation, wrong authority, signature/reason mutation, duplicate admission, unknown status targets, stale observer trust, and suspended organizations; closed registry/transition/quorum schemas, 76 MCP tools, and trust-state-only Action composition expose the same contract |
| Approval gossip organization trust registry | A separately trusted Ed25519 authority advances an ordered approval-log observer registry through exact generation- and digest-chained admissions, immediate suspensions, and permanent revocations; each admission pins the complete rotatable observer trust-state digest and every eligible quorum pins the registry identity, generation, and digest | Rust and CLI E2E cover admission and status governance, replay/fork rejection, rotated trust-state binding, suspension denial, permanent revocation, atomic writes, and closed registry/transition/quorum schemas; 109 MCP tools and the trust-state-only 234-input/91-output Action expose the same fail-closed contract |
| Approval gossip registry authority key rotation | Trust-root changes occupy the next generation in the same approval registry chain and preserve every observer admission and organization status; one domain-separated payload binds registry identity, prior digest, old/new Ed25519 keys, and monotonic time while requiring old-key authorization and successor-key possession | Rust and CLI E2E cover preserved decisions, post-rotation governance, replay, fork, wrong-old-key, same-key, new-signature mutation, atomic registry/key outputs, and a closed schema; 111 MCP tools and optional rotation application in the 235-input/91-output Action expose the transition without private keys |
| Approval gossip registry threshold governance | The retained root signs an exact threshold over ordered distinct authority identities and keys; every registry decision binds that governance digest into the existing generation/digest chain and requires a valid distinct-key quorum, after which root-only operations lock out | Rust and CLI E2E reject insufficient, duplicate, untrusted, or key-substituted authorities, signature mutation, replay, root-only membership bypass, and root-only key rotation; closed governance/transition schemas, 114 MCP tools, and paired inputs in the 237-input/91-output Action expose the fail-closed boundary |
| Approval gossip registry governance rotation | One domain-separated generation transition binds the retained and successor root-signed governance digests into the registry chain; both policies independently enforce their configured distinct-identity/key quorums before authority membership, keys, or threshold can change | Rust and CLI E2E preserve organization decisions, reject missing old/new quorum, policy/key substitution, signature mutation, stale-policy reuse, replay, forks, and time rollback; a closed schema, 116 MCP tools, and optional paired rotation inputs in the 239-input/91-output Action expose atomic replacement |
| Governed approval gossip registry root rotation | A prospective root proves private-key possession by signing successor governance, then retained and successor quorums approve one generation transition that atomically replaces both registry root and active governance while preserving organization state | Rust and CLI E2E reject missing old/new quorum, forged or stale successor policy, root/policy/key substitution, same-root rotation, signature mutation, replay, forks, and time rollback; a closed schema, 119 MCP tools, and mutually exclusive governed/root-only rotation in the 240-input/91-output Action expose recovery without bypass |
| Complete approval gossip registry history audit | A bounded typed event stream begins with an empty generation-zero registry and replays root transitions, dual-signed root rotations, threshold transitions, governance rotations, and governed root rotations through the production verifiers; the report binds every event and resulting state digest and emits the computed final registry | Rust and CLI E2E cover all five event kinds and reject reordering, replay, chain-breaking omission, and non-genesis starts without partial outputs; closed history/report schemas, 120 MCP tools, and mutually exclusive copied/history trust roots in the 241-input/93-output Action prevent copied snapshots from becoming trust anchors while explicitly leaving latest-head proof to witnessed checkpoints |
| Witnessed approval gossip registry history checkpoints | A retained-root signature binds one complete-history audit, computed final registry, final transition, active governance, root, generation, and issuance time; immutable local trust rejects rollback, truncation, and same-generation equivocation, while fresh independent witnesses bind one exact checkpoint | Rust tests cover root signing, acceptance, idempotence, alternate same-generation rejection, freshness, distinct identity/key quorum, and below-threshold evidence; closed checkpoint/trust/witness/quorum schemas, 124 MCP tools, and the 249-input/96-output Action expose verification without accepting private keys |
| Approval registry history witness key rotation | Immutable trust snapshots bind each witness identity to its current Ed25519 key, generation, prior rotation digest, and monotonic rotation time; every one-generation transition requires retained-key authorization and successor-key possession | Rust and CLI E2E cover chained trust, replay rejection, current-key export, trust-state-bound quorum, closed trust/rotation schemas, 128 MCP tools, and mutually exclusive direct/trust-state modes in the 250-input/96-output Action |
| Remote approval registry history checkpoint witnesses | Accepted complete-history checkpoint trust states are posted to bounded HTTPS services and returned witnesses are retained only after freshness, registry, generation, checkpoint, identity/key, and Ed25519 verification | Unit and real HTTP-stack CLI E2E cover the closed protocol, rotatable witness trust, atomic witness/receipt retention, HTTPS-only/no-redirect/1 MiB transport policy, trust-generation-bound receipts, one open-world task-forbidden MCP tool, and local/remote quorum composition through the 256-input/98-output Action |
| Approval registry witness receipt transparency | Structurally validated remote approval registry-history witness receipts normalize into the signed append-only approval transparency chain, binding the exact receipt, checkpoint, request, response, and witness identity before existing anchor, consistency, gossip, and witness controls apply | CLI real-HTTP E2E appends a rotated-trust-generation receipt, signs and verifies its log checkpoint, rejects a false verification decision without partial output, and confirms stable event bindings; the 129-tool MCP inventory and 256-input/98-output Action reuse the existing transparency controls |
| Verifier-bound approval registry receipt admission | A dedicated immutable-admission path reconstructs the exact request from retained checkpoint trust, verifies the exact response bytes, requires matching direct or generation-bound witness trust, and re-verifies freshness and the returned Ed25519 witness before append | Real-HTTP CLI E2E admits a retained rotated-key response and rejects false receipt decisions, response substitution, and stale admission without partial output; the 130-tool MCP inventory exposes the same mutually exclusive direct/trust-state boundary |
| Verifier-bound approval registry receipt admission quorum | One admission transaction independently replays every retained receipt against the same checkpoint trust, exact response bytes, admission clock, and identity-matched direct or rotatable witness trust; distinct witness identities, keys, receipts, and responses must meet a configurable threshold before canonically ordered append and a closed quorum report are written atomically | Real-HTTP CLI E2E covers order-independent trust matching, canonical member/event order, direct and generation-bound trust modes, report schema validation, and below-threshold rejection without log or report output; the 131-tool MCP inventory exposes the same task-forbidden boundary |
| Quorum-bound approval receipt-log signing | Successful admission reports bind the exact resulting log identity, entry count, head, complete digest, and every request/response/receipt/witness suffix member; a dedicated signing gate refuses partial, extended, substituted, legacy-unbound, or unrelated logs before reading a checkpoint output into existence | Real-HTTP CLI E2E signs the exact two-witness result and rejects an empty-log substitution without output; the 132-tool MCP inventory exposes the same task-forbidden gate while generic signing remains available for non-receipt logs |
| Domain-separated signed approval receipt-quorum checkpoints | A dedicated Ed25519 contract signs the exact normalized quorum-report digest, registry identity/generation/checkpoint, approval-log identity/count/head/digest, enforced threshold/result, and signer under a receipt-quorum-specific domain that cannot be substituted with a generic approval checkpoint | Real-HTTP CLI E2E signs and verifies the exact two-witness evidence, validates a closed checkpoint schema, and rejects threshold/signature mutation without output; two task-forbidden MCP tools bring the inventory to 134 |
| Independent approval receipt-quorum checkpoint witnesses | Every witness operation first re-verifies the exact quorum report, approval log, dedicated checkpoint signature, and trusted issuer key, then signs the normalized checkpoint digest under a separate witness domain; quorum verification enforces 24-hour freshness and distinct trusted witness identities and keys | Real-HTTP CLI E2E creates a two-witness quorum, validates the closed witness schema, and rejects evidence mutation without output; two task-forbidden MCP tools bring the inventory to 136 |
| Approval receipt-quorum checkpoint witness key rotation | Immutable trust snapshots bind each dedicated-checkpoint witness identity to one current Ed25519 key, generation, previous rotation digest, and monotonic rotation time; each exact one-generation transition requires retained-key authorization and successor-key possession | Rust and real-HTTP CLI E2E cover dual-signed rotation, replay and mutation rejection, rotated-trust quorum, closed trust/rotation schemas, and direct/trust-state verification modes; four MCP tools bring the inventory to 140 |
| Lifecycle gossip registry authority key rotation | Registry trust-root changes occupy the next generation in the same admission/status transition chain and preserve every organization decision; a domain-separated transition binds registry identity, prior digest, old/new Ed25519 keys, and monotonic time while requiring both old-key authorization and new-key possession | Rust and CLI E2E cover chained rotation, post-rotation governance, replay, fork, skipped generation, registry substitution, wrong-old-key, same-key, signature mutation, time reversal, atomic key export, closed schema, 78 MCP tools, and optional Action rotation application |
| Threshold lifecycle gossip registry governance | The retained registry root signs an exact configurable threshold over ordered distinct authority identities and Ed25519 keys; every admission, suspension, and revocation binds that governance digest into the existing generation/digest chain and requires a valid distinct-key quorum | Rust and CLI E2E reject insufficient or duplicate authorities, untrusted/key-substituted signers, root/policy substitution, signature mutation, replay, fork, and time reversal; closed schemas, 81 MCP tools, and paired Action governance/transition inputs expose the same fail-closed contract |
| Dual-quorum registry governance rotation | A domain-separated one-generation transition binds the retained and successor root-signed governance digests into the registry chain; both policies must independently meet their configured distinct-identity thresholds before membership, threshold, or governance keys change | Rust and CLI E2E reject missing old/new quorum, policy substitution, duplicate keys, signature mutation, stale successor policy, replay, fork, and time rollback; closed schema, 83 MCP tools, and atomic Action rotation composition expose the transition |
| Retained active registry governance | The first threshold transition pins its exact governance digest in registry state; later threshold changes require that digest and dual-quorum rotation atomically replaces it, while root-only membership and authority-key changes lock out after activation | Rust and CLI E2E reject a distinct validly root-signed policy, stale-policy application, root-only membership bypass, root-only key rotation, old-policy reuse after rotation, replay, fork, and digest mutation; the closed registry schema, existing 83 MCP tools, and 193-input/76-output Action carry the retained trust pointer through every consumer |
| Governed registry root rotation | A prospective root proves private-key possession by signing the successor governance policy, then retained and successor authority quorums independently approve one domain-separated transition that atomically replaces both registry root and active governance digest while preserving organization state | Rust and CLI E2E reject missing old/new quorum, forged or stale successor root policy, root/policy/key substitution, duplicate identities or keys, signature mutation, replay, fork, skipped generation, and time rollback; closed schema, 86 MCP tools, and mutually exclusive 194-input/76-output Action composition expose recovery without a root-only bypass |
| Complete registry history audit | A typed bounded event stream starts from an empty genesis registry and replays legacy root changes, root rotations, threshold transitions, governance rotations, and governed root rotations through the same production verifiers; the report binds every event and resulting state digest and emits the computed final registry | Rust and CLI E2E cover all five event kinds, reject reordering, replay, chain-breaking omission, fork, stale policy, bad quorum, signature mutation, and non-genesis starts without writing partial outputs; closed history/report schemas, 87 MCP tools, and mutually exclusive 195-input/78-output Action composition prevent copied snapshots from becoming trust anchors while explicitly leaving latest-head proof to witnessed checkpoints |
| Witnessed registry history checkpoints | A retained-root signature binds one complete-history audit, computed registry, final transition, active governance, retained root, generation, and issuance time; an immutable local trust state rejects rollback, truncation, and same-generation equivocation, while fresh independent witnesses bind one exact checkpoint | Rust and CLI E2E cover root signing, initial acceptance, exact-state pinning, alternate valid-history rejection, deterministic distinct-witness quorum, insufficient-quorum atomic failure, and closed checkpoint/trust/witness/quorum schemas; 91 MCP tools and fail-closed 203-input/81-output Action composition expose the same controls without storing private keys |
| Registry history witness key rotation | Immutable witness trust snapshots bind identity, current Ed25519 key, generation, prior rotation digest, and monotonic rotation time; every exact one-generation transition requires retained-key authorization and successor-key possession under a registry-history-specific domain | Rust and CLI E2E cover rotation, chained trust, replay rejection, current-key export, trust-state-bound quorum, closed trust/rotation schemas, 95 MCP tools, and mutually exclusive direct/trust-state modes in the 204-input/81-output Action |
| Remote registry history checkpoint witnesses | Accepted complete-history checkpoint trust states are posted to bounded HTTPS services and returned witnesses are retained only after freshness, checkpoint binding, identity/key binding, and Ed25519 verification | Real HTTP-stack CLI E2E covers exact protocol/checkpoint binding, rotatable witness trust, atomic witness/receipt retention, HTTPS-only/no-redirect/1 MiB transport policy, closed hash-bound receipts, one open-world task-forbidden MCP tool, and local/remote quorum composition through the 210-input/83-output Action |
| Registry witness receipt transparency | Structurally validated remote registry-history witness receipts normalize into the existing append-only transparency event chain, whose exact head and complete digest can be signed, publicly anchored, remotely witnessed, key-rotated, and quorum-verified by the admitting trust domain | CLI real-HTTP E2E appends a trust-generation-bound receipt, signs and verifies its log checkpoint, rejects a false verification decision before output, and confirms the event binds receipt/checkpoint/request/response identities; MCP and the 210-input/83-output Action reuse the same transparency controls |
| Manufacturing feedback ingestion | Closed fabrication declarations bind manufacturer disposition, stable findings, finite measurements, the exact analyzed board, run manifest, and raw inspection artifacts into JSON, Markdown, and SARIF; baseline comparison gates new actionable findings, severity escalation, disposition degradation, and pass-to-fail transitions | Unit and CLI E2E cover strict parsing, deterministic output, board/manifest identity, missing evidence, failed-gate artifact retention, comparison regressions, Action propagation, and MCP schemas |
| Governed feedback-to-policy recommendations | Recurring warning/error measurements can propose only stricter track-width, clearance, drill, or annular-ring minima; every feedback is rebound to its exact analyze-kicad manifest and target DFM profile, unsupported findings retain skip reasons, and reports are permanently proposal-only, human-gated, non-relaxing evidence rather than applicable policy | Unit and CLI E2E cover two-record thresholds, exact profile and manifest binding, unit conversion, insufficient evidence, governance tampering, closed nested schemas, overwrite refusal, Action artifact propagation, and MCP task/safety metadata |
| Schematic semantic diff | Symbol UUIDs, pin identities, normalized labeled/unnamed net identities, electrical attributes, and connectivity produce deterministic JSON, Markdown, and SARIF impact evidence while drawing-only coordinate/order changes are ignored; incomplete importer coverage always requires review | Unit and CLI E2E cover drawing equivalence, value and pin-type changes, connectivity movement, stable affected references, closed schema, post-write review gating, Action propagation, and MCP task metadata |
| Multi-reviewer AI approval quorum | Every independently signed response is freshly verified against one exact review request and organization trust root; duplicate signer, key, or response identities fail closed while configurable approval, provider, and model thresholds produce deterministic JSON and Markdown evidence before gating | Unit and CLI E2E cover two-provider approval, threshold failure with retained reports, duplicate-response and signature-tamper rejection, stable member ordering, closed schema, Action propagation, and MCP safety metadata |
| Risk-based AI reviewer routing | A strict organization policy maps freshly recomputed semantic schematic changes to specialist provider/model profiles and minimum reviewer counts; every otherwise unmatched or importer-coverage change is assigned to a mandatory fallback profile, and the deterministic plan binds both schematic digests plus the normalized policy digest | Unit and CLI E2E cover specialist and fallback routing, strict policy rejection, stable evidence, closed policy/plan schemas, Action outputs and post-publication gating, capability discovery, and MCP task/safety metadata |
| Profile-aware AI approval quorum | Reviewer routing is freshly recomputed from baseline/current schematics and bound to the exact AI review-request schematic; only cryptographically verified approvals whose full provider/model/version identity is an allowed candidate satisfy each active profile threshold | Unit and CLI E2E cover matching and unrelated models, retained profile-shortage evidence despite a passing global quorum, mismatched-schematic rejection, closed schema, Action composition, and MCP arguments |
| MCP stdio server | MCP 2025-11-25 lifecycle with older-version negotiation exposes typed DFM discovery, KiCad analysis, bundle comparison, and routing tools with structured results and safety annotations | Protocol lifecycle/error unit tests plus subprocess E2E proving clean JSON-RPC output and retained artifacts after a failed analysis gate |
| MCP asynchronous tasks | MCP 2025-11-25 Tasks provide bounded deferred execution, status polling, listing, blocking result retrieval, TTL expiry, and process cancellation for analysis, comparison, and routing while older clients remain synchronous | Unit tests cover capability negotiation, task support metadata, state transitions, original error propagation, cancellation, listing, and compatibility; subprocess E2E retrieves a failed-gate artifact bundle through a task |
| Bounded Rust CLI I/O and subprocess execution | Generic command files are regular, non-symlink, identity-checked, and capped at 128 MiB; per-file outputs are staged and atomically replaced, MCP frames are capped at 16 MiB, and doctor/KiCad/MCP children have fixed deadlines, concurrent output ceilings, cancellation, and Unix process-group or post-assignment Windows Job Object cleanup; a Windows assignment error is accepted only after the direct child is observed exited, without a descendant guarantee for that narrow race | Exact/overflow, UTF-8, symlink, atomic sentinel, timeout, cancellation, output-flood, descendant-cleanup, DRC-failure, MCP framing, TTL, Linux unit/E2E, native firmware smoke, and Windows runtime regressions plus real-runner checks cover the shared boundaries; workflow and release-script outer limits are independently audited |
| Bounded Python agent I/O and subprocess execution | Every production agent content path uses explicit regular-file and symlink/reparse-safe byte ceilings with strict UTF-8 and synchronized atomic publication; provider prompts and managed request bodies are capped before execution/network access; provider, pcbex, and KiCad children run without a shell under independent output caps, one input-inclusive deadline, and POSIX process-group or post-assignment Windows Job Object cleanup; Darwin's transient exited/zombie-only group `EPERM` is accepted only after a deadline-clipped direct-child reap and positive signal-zero `ESRCH` proof, without retrying group termination; ambiguous KiCad DRC reports fail closed | Exact/overflow input and output, invalid UTF-8, symlink/reparse and replacement races, no-clobber and sentinel preservation, cleanup-error chaining, bounded Darwin exited-group poll/probe races, live/existing/unauthorized group probes, simultaneous output flood, blocked-input timeout, nonzero compatibility, descendant cleanup, provider/managed-provider preflight and partial-publication behavior, executor output caps, DRC report validity, and repair candidate preservation run on Linux, with all boundary suites repeated on macOS and Windows without third-party dependencies |
| Bounded release and CI execution | Every workflow job has an explicit timeout and concurrency policy, every matrix has two-run parallelism, fuzzing caps campaign/input time, RSS, input bytes, and uploaded failure trees, the composite action supervises its complete analysis and gates publication on a link-safe 4,096-entry/512 MiB output tree, and release/completion/comment helpers bound aggregate time, process/API output, roadmap work, files, and atomic publication. Before publishing a tag, the release audit also requires the tagged SHA's latest Rust, Python, KiCad E2E, and Deterministic Pipeline check runs to be completed successfully by the pinned GitHub Actions app | Dependency-free policy tests cover the closed workflow/job inventory, exact timeouts, concurrency and matrices, fuzz flags, serialized release behavior, required-run gating, fixture cleanup, and action publication gate; script tests cover exact/overflow bytes, deadlines, nonzero children, HTTP aggregate limits, unsafe paths, tree depth/count/size/link rejection, bounded/malformed/failed check-run responses, release assets, and atomic sentinel preservation. The shared runtime suite is repeated on macOS and Windows, while a separate required Windows job runs the Rust process-boundary regressions in parallel; the aggregate Python check requires both |
| Placement candidate portfolio | Parallel deterministic generation explores balanced, wirelength, routability, constraint, and legalization objectives, computes the raw five-metric Pareto front, selects under caller base weights, and writes JSON plus KiCad candidate bundles | Core tests prove 1-vs-4-worker identity and Pareto dominance; subprocess E2E verifies reproducible JSON portfolios, selected artifacts, and KiCad boards |
| Routing candidate portfolio | Bounded parallel N-best routing explores balanced, shortest, via-minimized, bend-minimized, and alternate-order searches, removes route duplicates from a four-metric Pareto front, selects under caller base costs, and writes JSON plus KiCad candidate bundles | Core tests prove 1-vs-4-worker identity, duplicate handling, Pareto dominance, and thread ceilings; subprocess E2E verifies reproducible JSON portfolios, selected artifacts, and KiCad boards |
| KiCad schematic electrical IR | KiCad 6–10 symbols, unit/convert pin definitions, electrical types, transformed coordinates, wires, junctions, labels, power symbols, no-connects, and deterministic connectivity normalize into a closed versioned JSON boundary with explicit hierarchy/bus/directive coverage gaps | Importer tests cover topology, crossing junctions, transforms, UUID identity, future formats, closed schema, and coverage; CLI E2E proves byte reproducibility and complete gating writes inspectable output, while the example loads under KiCad 10.0.5 |
| Deterministic schematic approval gate | A closed versioned policy controls 16 electrical rules for coverage, annotation, footprints, no-connects, connectivity, driver conflicts, undriven inputs, net naming, power-metadata validity, rail conflicts, over-voltage, and required decoupling; all 12 default-error rules form an immutable floor across direct/signed policies, waivers, and baseline gates; reports bind stable findings and the approval decision to canonical schematic/policy SHA-256 identities | Table-driven unit tests reject disablement and both demotions for every floor rule while preserving advisory overrides and waivers; CLI E2E proves unsafe direct and packed policies fail before output, floor waivers are rejected, retained floor errors fail baselines, clean reviews are byte-identical, and schemas encode the floor |
| Bound simulation evidence | Simulator-independent bounded assertions and streamed raw-artifact SHA-256 descriptors bind DC, AC, transient, SI, PI, thermal, or custom results to the exact normalized schematic and electrical review; overall pass requires prior electrical approval and every measurement bound | Unit tests cover deterministic pass/fail, electrical rejection, malformed bounds, duplicate/empty artifacts, and closed schemas; CLI E2E proves raw digesting, byte reproducibility, identity binding, and fail-after-write gating |
| AI schematic review and signed approval | A closed evidence-ID-constrained AI contract assesses every explicit requirement, while fresh ERC/policy/simulation recomputation remains authoritative; deterministic Ed25519 envelopes bind request, model response, decision, failures, and signer, and verification requires a separately trusted public key | Rust tests prove approved/rejected signing, strict verification, wrong-key/tamper detection, forged-review rejection, complete requirement coverage, and closed schemas; Python injected-model tests reject invented evidence; CLI E2E covers protected key generation and the full prepare/sign/verify flow; MCP exposes approval-aware tools |
| Boardless AI schematic approval Action | A focused public Action binds a live KiCad schematic's semantic IR and freshly recomputed electrical review to one schema-v1 request without artifact-path binding, then re-verifies paired signed responses under policy-controlled signer, reviewer, provider, model, and optional session quorum without accepting a PCB, provider credential, or signing secret; ordered bounded inputs are stable-snapshotted around verification, and a fixed numeric-only report-derived summary, closed member invariants, and unchanged quorum decision are revalidated before publication | Core and CLI tests accept formatting-only source changes and reject semantic substitution or bound request schemas before output; focused fake-runner contract tests cover paired path forwarding, per-file and aggregate input limits, linked and mutated inputs, hostile summary replacement, closed report identities and member results, bounded no-clobber output, valid rejection retention, invalid-evidence cleanup, quorum replacement, and scan/revalidation/upload/final-gate wiring, while the CI smoke uses real signed session evidence and confirms a needs-human vote is not counted as approval |
| AI schematic approval CLI/MCP live-binding parity | The schema-v1 live KiCad schematic binding is available to single `verify-ai-approval --schematic` and MCP `verify_schematic_approval`/`verify_schematic_approval_quorum`; each path imports the bounded source and freshly recomputes the deterministic electrical review before accepting signature or quorum evidence, while generated/native artifact modes remain mutually exclusive | Core and CLI regressions cover formatting-equivalent source acceptance, semantic substitution, fresh-review mismatch rejection, and schema-v1 gating; MCP unit and real-stdio E2E cover literal forwarding, mixed-mode rejection, single-signature success and semantic rejection, and retained live-source quorum success while schema-v2 through v4 artifact-bound paths remain unchanged |
| AI schematic approval live signing parity | The schema-v1 live KiCad schematic binding is available to `sign-ai-review --schematic` and MCP `sign_schematic_approval`; each path imports the bounded source, compares semantic IR, and freshly recomputes the deterministic electrical review before reading a private key or creating an approval, while request/response/signature wire schemas remain unchanged and schema-v2 through v4 continue to use artifact paths only; signing is documented for an isolated workspace because bounded reads detect observed replacements but do not claim a race-free filesystem boundary | Core verification recomputes and compares the fresh electrical review; CLI regressions cover formatting-equivalent source acceptance, semantic substitution rejection before key access, schema-v1 gating, mixed-mode rejection, session compatibility, and unchanged schema-v2 through v4 artifact signing; MCP schema/bridge and real-stdio E2E cover task prohibition, literal live-source forwarding, private-key ordering, and live single-signature success/rejection |
| AI schematic approval signing preflight and atomic publication | Trusted CLI and MCP signing validate the complete public response, signer, optional session/request binding, selected live or artifact-bound evidence, and no-clobber destination before opening a private key; a valid rejected review is still signed and published, while an existing file, symlink, or other non-regular destination remains untouched; staged approval publication is atomic at the destination-file boundary, but verification, key access, and publication are not one filesystem transaction; request/response/session/signed-approval schemas and schema-v2 through v4 artifact paths remain unchanged, and root-Action live quorum is deferred | CLI regressions cover invalid response/signer/session/output preflight without secret access, accepted rejected-envelope publication before `--require-approved`, destination collision/symlink preservation, and valid approval publication; MCP bridge/stdio regressions cover the same ordering and no-clobber contract, while schema and artifact-bound compatibility suites remain green |
| Root Action live AI schematic quorum | The optional `ai-review-schematic` input selects schema-v1 `verify-ai-quorum --schematic`, freshly binds the live semantic IR and electrical review, and publishes a distinct live-verification result; the complete quorum and policy set is required, schema-v2-through-v4 generated/native/deterministic artifact inputs are mutually exclusive, and their existing output retains its prior meaning; the broader root workflow uses CLI bounded stable reads rather than the focused Action's aggregate snapshot boundary | Thirty-five deterministic-pipeline Action tests cover live forwarding, option-like and bounded paths, session/default-KiCad compatibility, complete inputs and policy, mixed-mode rejection, distinct output, and legacy behavior; static Action/YAML and CI-policy suites remain green, while the public CI smoke exercises real signed session evidence together with general schematic reviewer routing and verifies the live/artifact output split |
| Exact AI review artifact binding | Opt-in request schema v2 covers one exact generated KiCad schematic plus raw/normalized deterministic plan, retained approved report, and run identities; prepare, sign, approval verification, and quorum verification stable-read and rerun the plan, byte-compare the report, and cross-bind its raw schematic and electrical-review handoff before any signature is accepted, while request schema v1 remains unchanged | Core hash/signature/schema/limit/null/duplicate regressions preserve the fixed v1 hash; CLI E2E accepts a byte-identical copied path and rejects one-byte mutation, stale reports, partial arguments, alternate raw review bytes, and independently valid mixed runs; MCP schemas/bridges, Python adapter boundaries, and Action ordering/output tests cover the same fail-closed contract |
| Native KiCad schematic ERC evidence | A shell-free bounded runner snapshots one exact schematic into private fixed-name staging, isolates supported KiCad configuration/profile paths, invokes fixed error-only JSON ERC, validates raw schema/duplicate keys/status consistency, strips volatile time/path data, and retains a closed deterministic report; request schema v3/binding v2 covers its exact bytes and run identity and requires fresh replay before every signature/quorum boundary while v1/v2 remain compatible | Native unit tests cover deterministic time stripping, exact argv, approved/rejected status mapping, malformed/duplicate/missing/stale reports and identity replay; CLI schema-v3 E2E covers prepare/sign/verify and report mutation; MCP Tasks/summary/group tests, Python closed-boundary tests, Action preflight/output tests, and real KiCad 10.0.5 E2E cover two-run identity plus retained four-error rejection |
| Standalone native KiCad ERC Action gate | The root composite Action can independently run error-only report v1 or warning-policy report v2, authenticate the compact runner summary against canonical retained bytes plus exact schematic/policy identities, publish twelve bounded outputs, and retain valid rejection evidence before optional final approval enforcement without joining the AI artifact flow | Dedicated helper and Action tests cover v1/v2 approval and rejection, exact counts/digests/canonical bytes, malformed/duplicate/type/boundary mismatches, stale and linked paths, option-like paths, invalid booleans, output-root injection, disabled compatibility, and post-publication gate wiring; existing real-KiCad E2E covers the underlying runner |
| Boardless native KiCad ERC Action | A focused public subdirectory Action requires only a workspace-confined regular schematic, optionally applies a similarly confined closed warning policy, authenticates and publishes the same twelve native identities, excludes glob interpretation from its literal output root, scans and uploads the isolated evidence tree, and defers optional approval enforcement to a final `always()` gate while the root Action remains board-required | Direct fake-runner tests cover v1/v2 approval and retained rejection, fatal/malformed/missing/digest failures, argv separation, invalid booleans, absolute/traversal/link/glob/stale boundaries, Job Summary, wrapper outcome, and the executable publication gate; KiCad 10 E2E runs the local composite Action through approved and rejected artifact-preserving paths |
| Standalone native KiCad ERC fresh replay | Read-only CLI, MCP, and focused-Action replay reruns canonical native ERC report schema v1 or warning-policy schema v2, stable-reads the original schematic around KiCad, compares fresh bytes with retained evidence, propagates Task cancellation, and retains rejected evidence before an optional approval gate without changing AI or native-run contracts | CLI/MCP/focused-Action regressions cover v1/v2 replay, retained-report and source/policy mutation, compact identity summaries, cancellation, no-clobber publication, rejected-evidence retention, and approval-gate ordering |
| Native KiCad PCB DRC evidence gate | Standalone CLI, MCP, focused Action, and opt-in root Action run fixed `drc.v1`-compatible native PCB DRC in private staging; optional same-stem or explicit project/rules identities are snapshotted, volatile date/path/UUID fields are removed, findings are canonicalized to integer-nanometre positions, fixed-v1 schematic parity is rejected, and strict zero error/warning approval retains source/run identities plus rejected evidence while integrations expose the exact report digest | Rust/CLI/MCP/Action tests cover KiCad 9/10-shaped JSON normalization, category/count reconciliation, companion discovery and binding, source/report digests, malformed/status mismatches, paths, bounds/no-clobber/retention, and approved/rejected gates; the real KiCad 10 E2E proves byte-identical clean reruns and retained rejection, while internal DRC, `drc.rpt`, ERC, AI approval, and pipeline/manufacturing integrations remain explicit later boundaries |
| Fresh native KiCad PCB DRC replay | A read-only CLI and MCP tool rerun the fixed native DRC against the current board and resolved project/rules companions, bounded-stable-read every source after KiCad exits, and byte-compare the canonical result with retained evidence; MCP task cancellation reaches the bounded KiCad process group directly, while the focused Action authenticates the compact summary, atomically copies the exact retained bytes into a fresh artifact directory, and preserves valid rejected evidence before an optional approval gate | CLI integration tests cover approved and rejected reports, fresh mismatches, board/project/rules/report mutation races, symlink/noncanonical evidence, and compact summaries; MCP stdio proves option-like paths, exact 17-field authentication, retained-byte stability, and cancellation without an orphaned KiCad process; focused-Action tests cover no-clobber publication, while KiCad 10 E2E reruns approved and rejected evidence and compares both report and run digests |
| Signed approval transparency log | Strictly parsed approval artifacts form immutable snapshots with monotonic sequence/time, previous-entry and self-digests; an Ed25519 checkpoint binds the full normalized log digest, head, entry count, log ID, and signer | Rust and CLI E2E reject mutation, reordering, deletion, truncation, stale checkpoints, time reversal, wrong keys, and signature tampering; Action smoke verifies the checkpoint and exposes the result; MCP exposes init, append, sign, and verify tools with destructive-action annotations |
| Approval transparency witness quorum | Independent Ed25519 witnesses bind the exact normalized checkpoint digest, log head/count, witness identity, and observation time; acceptance requires distinct trusted witness identities and keys under a configurable threshold of at least two | Rust and CLI E2E prove two-witness acceptance plus stale-checkpoint replay, duplicate identity/key, wrong-key, and below-threshold handling; Action and MCP expose paired witness inputs and fail-after-report quorum gates |
| Remote approval transparency witness | A synchronous HTTPS boundary posts one closed checkpoint request without redirects, bounds total time and response bytes, keeps optional Bearer values out of argv/evidence, and atomically retains a witness plus hash-bound transport receipt only after trusted-key signature verification | Loopback HTTP is available solely behind a hidden test flag; CLI E2E exercises the real HTTP stack, authorization delivery, strict request/response binding, immediate cryptographic verification, receipt output, and plain-HTTP rejection; Action smoke acquires a remote witness and includes it in the three-member quorum; MCP exposes only HTTPS |
| Approval transparency witness key rotation | Immutable trust-state snapshots pin witness identity, generation, current Ed25519 key, prior rotation digest, and monotonic rotation timestamp; each exact one-generation transition carries old-key authorization plus new-key possession proof under a domain-separated payload | Rust and CLI E2E cover two-step rotation, validated key export, replay, rollback, key substitution, wrong-old-key, same-key, fork, and time-reversal rejection; the Action accepts trust state for remote witnesses and MCP exposes initialize/sign/apply/export operations |
| Approval transparency public-log anchoring | Domain-separated checkpoint leaves use RFC 6962 tree splitting and bounded audit paths; a separately trusted Ed25519 tree-head signature binds log identity, exact tree size, root digest, and observation time | Core and CLI E2E verify odd-sized tree inclusion and reject checkpoint, sibling, index, root, signature, and trust-key substitution; Action smoke enforces the anchor after artifact publication and MCP exposes proof creation and verification |
| Approval transparency public-log consistency | Signed retained/current tree heads and a logarithmic RFC 6962 consistency path prove exact prefix extension without transferring the complete newer receipt log to each verifier | Core and CLI E2E cover valid extension, path tampering, rollback, equal-size equivocation, observation-time reversal, log/key substitution, atomic reports, closed schemas, MCP create/verify tools, and an Action fail-closed consistency gate |
| Approval transparency public-log gossip | Independent observer signatures bind one trusted signed tree head, observer identity, log key, and a seven-day-bounded validity window; consumers compare same or differently sized views using exact prefix proofs | Core and CLI E2E cover same-tree and cross-size observations, expiry, not-yet-valid evidence, missing/redundant/wrong proofs, observer/log key substitution, equal-size split views, closed schemas, MCP sign/verify tools, and an Action fail-closed gossip gate |
| Central policy-pack retrieval | A bounded HTTPS GET follows no redirects, rejects query/userinfo and plain HTTP, keeps optional Bearer values out of argv/evidence, and accepts a registry response only after strict parsing, Ed25519 verification, and monotonic trust-state advancement | Real HTTP-stack CLI E2E verifies authorization delivery and atomic signed/normalized/state/receipt retention, rejects rollback before output, and reserves loopback HTTP for a hidden test flag; Action smoke fetches and analyzes with the remote pack while MCP exposes HTTPS only |
| Roadmap and release governance | A closed machine-readable milestone ledger binds every production-pipeline capability to a release; publication is blocked until the tag, exact 12-asset contract, archive SHA-256 files, and SPDX documents pass an independent post-build audit | Ten audit tests cover closed/ordered roadmaps, exact assets, draft lookup, checksum tampering, SPDX validation, and protection-policy rejection; the same auditor passes against the live v1.308.0 release and verifies strict protected-main settings |
| Performance regression gate | Deterministic search-state budgets for 10 parallel nets and a 100 mm board with 200 obstacles | Dedicated `performance_budget` target runs on every PR |
| Deterministic parallel candidate search | Configurable 1–8 bounded workers explore first-pass A* candidates; ordered validation reuses one board snapshot per pass and sequential conflict fallback controls commits | Ten-net 1-vs-8-worker regression is byte-identical; Criterion measures actual 1/2/4/8-worker wall time |
| Automatic local push-and-shove | Grid-aligned route-interior translation keeps terminal anchors fixed; failed nets try bounded blocker shoves before selective rip-up and atomically accept only checked combined routes | Successful manual shove preserves terminals, invalid edits roll back, and automatic blocker recovery produces a clean two-route board |
| Complete Rule Area constraints | Track, via, copper-pour, and footprint prohibitions are independent; local minimum width/clearance profiles participate in routing and DRC | KiCad selective Rule Area retains footprint/via-only flags; undersized nets route around a local profile and existing footprint/track violations are reported |
| PDN design checks | Per-power-net current, voltage-drop budget, and parallel-via minimum use routed geometry and stackup copper thickness for DC resistance estimation | 1 A narrow-trace regression reports both excessive voltage drop and insufficient vias |
| Decoupling placement quality | Dedicated capacitor-anchor to IC-power-pin distance and same-side constraint uses transformed named anchors | Colocated anchors score clean on one side and receive a deterministic penalty across board sides |
| Practical board regression corpus | Anonymized USB differential, four-layer power/inner-signal, eight-net BGA fanout, and reproducibly generated 100-net six-layer backplane topologies | Clean, byte-idempotent routing with per-fixture search budgets on every PR; large fixture completes in 46,500 states under a 100,000-state ceiling |
| BGA escape routing | Deterministic radial/row/column/four-way dog-bone stubs, optional via-grid snapping, multi-ring collision fallback, stackup-aware fanout vias, and inner-layer continuation before global routing | Two-net BGA regression blocks every first-ring site, checks grid-aligned second-ring microvias, front stubs, inner tracks, and full-board cleanliness |
| Return-path control | Per-signal/reference-net transition rules check maximum stitching-via distance, sample stackup-selected reference fills for split-plane/slot crossings, and add fully checked Zone-connected or track-connected reference vias | Filled-plane gap regression detects a slot; two-layer transitions insert legal direct-to-Zone and track-connected GND stitches and finish DRC-clean |
| Automatic rounded routing | Orthogonal corners are trimmed into tangent quarter-circle native arcs at the routing-grid radius, with whole-board acceptance checks and reporting | Right-angle regression verifies valid arc geometry, preserved connectivity, and a clean full-board check |
| Stackup impedance constraints | KiCad stackup import captures copper thickness plus dielectric height/permittivity and reference planes on both sides; DRC selects microstrip or symmetric/asymmetric embedded geometry | Four-layer import verifies both references and physical dimensions; controlled-impedance regression checks shifted targets; embedded estimators cover symmetric and asymmetric geometry |
| Advanced length tuning | Length groups constrain meander amplitude, pitch, and up to 16 distributed tuning sections; each incremental section is whole-board checked | Three-section regression adds exactly 3 mm across separate legal spans and bus-skew regression remains clean |
| KiCad placement I/O | Footprints, pad-net connections, locked state, origin-aware position/rotation write-back, and `place-kicad` CLI | Locked/rotated/non-zero-origin round-trip regression and CLI integration test |
| Multilayer routing | `In1.Cu`–`In30.Cu` model/serde, KiCad layer-table and item I/O, all-layer through vias, and per-net layer constraints | Forced inner-layer core regression, four-layer importer test, and real-KiCad four-layer DRC/idempotence fixture |
| Differential-pair rules and checking | Explicit pair model, KiCad net-class inference, differential width, skew/coupling/layer-via symmetry checks | Coupled/skew core regression and KiCad `_P`/`_N` inference test |
| Differential impedance DRC | Pair-level target/tolerance applies an edge-coupled microstrip odd-mode correction to copper-aware single-ended impedance | Calculated target passes; a 20 Ω target shift produces a dedicated differential-impedance violation |
| Impedance width solver | `impedance-width` reverse-solves single-ended or differential trace width from a board stackup, layer, target, optional pair gap, and bounded manufacturing range | Core regressions round-trip solved widths through both estimators; CLI parsing covers inner-layer differential arguments |
| Impedance transition DRC | Per-net-class maximum step compares stackup-derived impedances of segments connected through each via, independently of absolute target tolerance | Two-layer regression uses different widths around a through-via and reports the dedicated `impedance_transition` rule |
| Differential impedance transition DRC | Pair-level maximum step evaluates every via on both members with the differential stackup model, independently of an absolute target | Symmetric two-member layer transition with different pre/post-via widths reports `differential_impedance_transition` |
| Board-wide impedance report | `impedance-report` emits JSON for every routed single-ended segment and differential member, with estimates, target deviations/pass state, via steps, and invalid-geometry count | Core report regression verifies evaluated geometry; CLI regression covers explicit report output |
| Impedance CI quality gate | Report-level counters aggregate invalid geometry, out-of-tolerance segments, and excessive single-ended/differential transitions; `--fail-on-violations` writes JSON before returning nonzero | Missing-stackup regression fails the clean-state predicate; counter-state and CLI flag regressions cover all gate plumbing |
| Impedance baseline regression gate | Deserializable reports compare summary counts, stable net/segment target deviation, and single-ended/differential member transition steps; `--baseline` fails CI after writing the current report | Baseline regression proves count, segment-deviation, and transition-step worsening are detected while improvements are accepted |
| Coupled differential-pair autorouting | Pair-terminal correspondence, full-route translation, and accept-only-on-clean fallback | 100%-coupled, zero-skew autorouting regression |
| Simultaneous differential-pair search | Pair-state A* validates both offset tracks at every step before committing either route | One-sided obstacle regression requires a joint detour while retaining 100% coupling |
| Synchronous differential-pair tuning | Pair-level minimum length, amplitude, pitch, and distributed-section controls transform both members atomically with whole-board acceptance | Equal-length translated pair gains a legal synchronized meander and remains DRC-clean |
| Length constraints and meander tuning | Net-class minimum/maximum length checks and deterministic legal orthogonal detours | Meander regression verifies the interval and a clean full-board check |
| Parallel-bus length matching | Named net-ID groups with maximum skew, group checking, and automatic tuning toward the longest member | Two-member bus regression ends within its declared skew and remains clean |
| Filled copper-zone interoperability | KiCad filled polygons become exact layer/net-owned copper and are preserved during route write-back | Filled GND-zone import regression |
| Native copper-zone generation | Net-owned polygon model with layer, clearance, and minimum thickness; native KiCad zone output and outline/fill import | Zone syntax, parameter, polygon, and owned-obstacle round-trip regression |
| Internal copper-zone fill | Conservative cell fill, foreign-copper/keepout clearance, pad thermal spokes, connected-component island removal, and filled-polygon export | Split-zone and thermal regression plus KiCad filled-polygon round trip |
| Via types and layer ranges | Through, blind/buried, and microvia model; KiCad round-trip; range-aware connectivity/clearance | Blind and adjacent-layer microvia regression |
| Automatic Via strategy | Stackup-aware micro, blind/buried, and through selection with span-aware collision reservation and cost | Transition classification and forced inner-layer autorouting regressions |
| Extended pad shapes | Roundrect, trapezoid, and custom polygon model/import with rotated exact polygon obstacles | Three-shape KiCad geometry regression |
| Practical placement constraints | Front/back side, allowed rotations, rectangular regions, named-anchor-aware board-edge distances, complete-component region containment, and KiCad courtyard dimensions | Region optimization, valid/unknown anchor handling without panics, and back-side courtyard regressions |
| Placement flips and exact courtyards | Side-aware pin mirroring, polygon collision/boundary scoring, and atomic KiCad footprint layer swap | Bounding-box false-positive, opposite-side, and front/back round-trip regressions |
| Spatially bounded rasterization | Conservative per-shape cell windows followed by exact predicates | Window regression and 100 mm/200-obstacle Criterion scenario |
| Configurable DFM checks | Width, actual copper clearance, drill, annular ring, aspect ratio, and copper-to-edge checks with JSON CLI report | Multi-rule manufacturing regression and `dfm` command |
| Extended DFM and SARIF | Exact circle/oval/custom Via-in-pad, drill spacing, acute junctions, and SARIF 2.1.0 output | Multi-violation geometry and SARIF result-count regression |
| Component and mounting-hole DFM | KiCad PTH/NPTH drill dimensions and plating are preserved; minimum drill, plated annular ring, aspect ratio, board-edge, and capsule-exact spacing checks include component holes and vias | Circular PTH and rotated oval NPTH import plus combined component-hole DFM regression |
| Component-hole model validation | Normal DRC rejects incomplete/non-positive drill dimensions and plated drills that do not fit inside their pads, independently of optional manufacturing rules | Invalid PTH and incomplete NPTH regression without manufacturing rules |
| Offset component drills | KiCad pad-local drill offsets are preserved and rotated into exact circular/oval hole geometry for DRC and DFM | Rotated offset capsule coordinates plus offset oval NPTH import regression |
| Exact plated-hole containment | Circular and oval pads validate the complete offset hole capsule against their curved boundary; rectangular pads validate capsule extents | Diagonal-offset circle rejection and valid rotated oval-slot containment regressions |
| Roundrect plated-hole containment | KiCad roundrect ratios are retained as corner radii and the complete hole capsule is tested against the radius-eroded curved boundary | 0.25-ratio import assertion and corner-offset hole rejection regression |
| Trapezoid plated-hole containment | KiCad `rect_delta` geometry is retained and each hole-capsule endpoint must clear every convex sloped edge by the drill radius | Delta import assertions and bounding-box false-negative rejection regression |
| Custom-polygon plated-hole containment | Complete circular/oval hole capsules require endpoint membership and exact radius clearance from every imported custom-pad polygon edge | Triangular custom-pad bounding-box false-negative regression |
| Custom-pad topology validation | Normal DRC rejects insufficient vertices, degenerate edges or area, and non-adjacent self-intersections before routing or hole geometry consumes custom polygons | Three malformed polygon regressions without manufacturing rules |
| Base pad geometry validation | Normal DRC rejects non-positive dimensions, non-finite rotations, out-of-range roundrect radii, and degenerate trapezoid deltas | Four malformed shape regressions without manufacturing rules |
| Pad layer membership validation | Normal DRC requires a non-empty, duplicate-free pad layer set drawn entirely from the board copper stackup | Empty, duplicate, unknown-inner-layer, and valid multilayer regressions |
| Pad net-reference validation | Normal DRC rejects pad net identifiers absent from the board net table while accepting mechanical and declared-net pads | Mechanical, declared-net, and undeclared-net regression |
| Net table identity validation | Normal DRC requires unique non-zero net IDs and unique non-empty names before routing, rule lookup, or pad ownership consumes the table | Zero ID, blank name, duplicate ID, and duplicate name regressions |
| Terminal layer membership validation | Normal DRC and Router require every net terminal to use a non-empty unique subset of the declared copper stackup | Empty, duplicate, undeclared, valid multilayer, and Router-construction regressions |
| Net-class reference validation | Normal DRC and Router require every optional net-class assignment to resolve against the board net-class table instead of silently applying base rules | Unassigned, declared, undeclared, and Router-construction regressions |
| Net-class dimension validation | Normal DRC independently requires positive track width/drill, non-negative clearance, and via diameter greater than drill for every declared class | Invalid width, clearance, drill, annulus, and valid unused-class regressions |
| Net-class layer membership validation | Normal DRC requires every optional class layer restriction to be a non-empty unique subset of the declared copper stackup while preserving omitted restrictions | Empty, duplicate, undeclared, unrestricted, and valid multilayer regressions |
| Net-class length-limit validation | Normal DRC requires optional minimum/maximum route lengths to be positive and ordered while preserving one-sided and unbounded limits | Zero minimum, negative maximum, reversed range, one-sided, bounded, and unbounded regressions |
| Net-class impedance-limit validation | Normal DRC requires target/tolerance pairing, finite values, positive targets, and non-negative tolerances/transition limits for every class | Missing pair member, zero/NaN target, negative/infinite tolerance, negative/NaN step, and valid optional combinations |
| Net-class differential-dimension validation | Normal DRC and Router require optional differential widths to be positive and differential gaps to be non-negative | Zero/negative width, negative/zero gap, valid pair, omitted values, and Router-construction regressions |
| Net-class name validation | Normal DRC and Router require every net-class table key to contain at least one non-whitespace character | Empty, whitespace-only, valid-name, and Router-construction regressions |
| Differential-pair identity validation | Normal DRC and Router require unique non-empty pair names, distinct declared member nets, and exclusive pair membership | Blank/duplicate names, self-pairing, unknown/reused members, valid pair, and Router-construction regressions |
| Route net-reference validation | Normal DRC rejects route net identifiers absent from the board net table before connectivity, width, or clearance checks consume the route | Declared and undeclared empty-route regression |
| Duplicate route validation | Normal DRC permits at most one route record per net so route indexing cannot silently hide additional copper | Two-route single-net regression with one explicit duplicate violation |
| Track segment geometry validation | Normal DRC requires distinct endpoints, positive width, and a declared copper layer before angle, boundary, or clearance evaluation | Zero-length, zero-width, unknown-layer, and valid segment regressions |
| Route arc geometry validation | Normal DRC requires positive width, a declared copper layer, and three points defining a curve; invalid arcs are excluded from DRC linearization | Zero-width, unknown-layer, repeated-point, collinear, and valid arc regressions |
| Via geometry validation | Normal DRC requires a positive drill diameter and a strictly larger outer diameter before layer, edge, minimum-size, or clearance checks consume the via | Zero-diameter, zero-drill, equal-diameter, oversized-drill, and valid-via regressions |
| Via layer-range validation | Normal DRC requires distinct declared endpoint layers and restricts microvias to adjacent copper layers before edge, size, or clearance checks consume the via | Unknown-layer, same-layer, non-adjacent microvia, adjacent microvia, and through-via regressions |
| Teardrop geometry validation | Normal DRC requires simple non-degenerate polygons on declared copper layers before converting teardrops into net-owned obstacles | Insufficient-point, repeated-edge, zero-area, unknown-layer, and valid-triangle regressions |
| Filled-zone geometry validation | Normal DRC requires every filled contour to be a simple non-degenerate polygon on a declared copper layer before conversion into a net-owned obstacle | Insufficient-point, repeated-edge, zero-area, unknown-layer, and valid-fill regressions |
| Zone outline validation | Normal DRC requires every source zone outline to be a simple non-degenerate polygon on a declared copper layer before refilling or plane analysis consumes it | Insufficient-point, repeated-edge, zero-area, unknown-layer, and valid-outline regressions |
| Zone rule-dimension validation | Normal DRC requires non-negative clearance/thermal gaps, positive minimum copper thickness, and positive spoke width when thermal relief is enabled | Negative-clearance/gap, zero-thickness/spoke, solid-fill, and valid-thermal regressions |
| Board dimension validation | Normal DRC independently requires positive board width and height, matching the routing precondition | Zero-width, negative-height, and valid-board regressions |
| Copper-layer table validation | Normal DRC independently requires a non-empty, duplicate-free table containing only supported front/back/internal copper layers | Empty, duplicate, unsupported `Inner(31)`, and valid-stackup regressions |
| Board outline validation | Normal DRC requires an explicit outline to be a simple non-degenerate polygon while preserving the empty-outline rectangular fallback | Insufficient-point, repeated-edge, zero-area, self-crossing, fallback, and valid-outline regressions |
| Board cutout topology validation | Normal DRC requires every cutout to be a simple non-degenerate polygon before board-boundary and copper-edge checks consume it | Insufficient-point, repeated-edge, zero-area, self-crossing, and valid-cutout regressions |
| Board cutout containment validation | Normal DRC requires every topologically valid cutout vertex to remain inside the effective explicit or rectangular board outline | Inside and partially outside cutout regressions |
| Board outline bounds validation | Normal DRC requires every topologically valid explicit-outline vertex to remain within the declared board width and height | Negative-coordinate, oversized-coordinate, and boundary-inclusive valid regressions |
| Routing-grid validation | Normal DRC independently requires a positive global routing grid, matching the routing precondition | Zero-grid, negative-grid, and valid-grid regressions |
| Base routing-rule validation | Normal DRC and Router require positive track width/drill, non-negative clearance, and via diameter greater than drill | Invalid width, clearance, drill, annulus, valid-rule, and Router regressions |
| Obstacle-layer validation | Normal DRC requires rectangular, round, capsule, polygon, and keepout obstacles to use a non-empty unique subset of the declared copper stackup | Empty, duplicate, undeclared, and valid layer-list regressions across all obstacle families |
| Obstacle net-reference validation | Normal DRC requires optional net ownership on rectangular, round, capsule, polygon, and keepout obstacles to resolve against the board net table | Unknown ownership across all five obstacle families plus declared ownership regression |
| Rectangular-obstacle geometry validation | Normal DRC and Router require strictly ordered minimum/maximum coordinates on both axes | Reversed-X, zero-height, valid rectangle, and Router-construction regressions |
| Curved-obstacle diameter validation | Normal DRC and Router require strictly positive diameters for round and capsule obstacles | Zero/negative and valid diameter regressions for both shapes plus Router-construction coverage |
| Polygon-obstacle topology validation | Normal DRC and Router require polygon obstacles to be simple and non-degenerate before rasterization or clearance checks consume them | Insufficient-point, repeated-edge, zero-area, self-crossing, valid-triangle, and Router regressions |
| Keepout-definition validation | Normal DRC and Router require simple non-degenerate keepout polygons with at least one prohibition/local rule and physically valid local dimensions | Malformed, inert, zero-width, negative-clearance, valid-local-rule, and Router regressions |
| KiCad route arcs and teardrops | Three-point copper-arc import/export with checker linearization and native pad/via teardrop zones | Arc coordinate round-trip and teardrop syntax regression |
| Automatic teardrop generation | Via/track junction taper geometry with boundary, foreign-copper, and duplicate rejection | Clean four-point taper and second-pass idempotence regression |
| Precise route-arc geometry | Analytical circumcircle/sweep length plus conservative 1 µm adaptive DRC envelope and curved SVG output | Semicircle length and midpoint-only collision regression |
| Self-updating completion audit | Version and discovered Rust/Python test totals generated between protected markers | `update-completion-audit.py --check` runs on every PR |
| KiCad end-to-end CI | KiCad 10 routing, DRC, second-pass idempotence, and retained diagnostics for three fixtures | Rectangular, non-rectangular, and polygon-keepout boards run on every PR |
| Bounded repair and score comparison | Iteration/item limits and non-regression acceptance | Bounded executor test |
| Digest-bound physical profiles | Closed bounded geometry/DFM profile applied atomically to placement, routing, analysis, and fabrication; raw and canonical SHA-256 bindings must match through manufacturing and pipeline verification | Core concave-outline/cutout/fixed-position/atomicity/non-relaxation tests, KiCad placement revalidation, manifest v1/v2 compatibility and substitution tests, MCP/Action forwarding tests |
| MCP/Action hardware pipeline parity | Opt-in composite Action pipeline inputs/outputs forward the generated analysis evidence and physical-profile binding; rejected pipeline reports are published before the final fail gate, while MCP `check_schematic`, `check_circuit_spec`, and `pipeline_verify` expose the same closed contract with optional Tasks support | Action static/input/output and retained-failure tests, MCP tool-schema/dispatch/optional-Tasks tests, and pipeline report publication regressions |
| Verified circuit-to-KiCad handoff | Native digest-bound verification compares a closed flat/single-unit circuit-spec v2 semantic subset with an existing KiCad schematic, runs both deterministic ERC/check paths, and retains the report before a required-approval rejection; it is not a generator | Circuit/KiCad handoff schema and CLI/MCP verification regressions, including source/canonical identity, mismatch, ERC, and rejection-retention coverage |
| Circuit-spec/KiCad schematic-board binding | Standalone native verification recalculates the raw v1.415 handoff and binds exact reference/footprint identity, value/MPN/BOM/DNP metadata, pin-to-pad number/net/no-connect state, and complete net/footprint/pad coverage to the actual board using canonical names from the imported schematic; source, canonical, and binding digests are deterministic and rejected reports are retained atomically | Board-binding schema and Rust/CLI/MCP regressions cover exact and duplicate/missing/extra records, terminal-less raw nets, reserved net 0, declared no-connect pads, the empty unconnected unnumbered-NPTH exception, nested-handoff rejection, bounded inputs, source/electrical digest behavior, output collisions, and retained failures |
| Bounded-input deterministic pipeline runner | One closed plan authorizes every raw circuit, schematic, board, review, analysis, manufacturing, firmware, and optional factory input by relative path, byte count, and SHA-256; private snapshots feed the standalone board-binding and existing pipeline gates in process, whose canonical schematic and raw board identities are cross-bound into one domain-separated no-side-effect report | Unit and CLI E2E cover closed schemas, unsafe paths and digest mismatches, exact original firmware-directory enforcement, stable staged basenames, independent gate evidence retention, output preflight/no-clobber behavior, complete end-to-end approval, and byte-identical repeated reports |
| MCP deterministic pipeline runner parity | The closed v1.417 runner is available synchronously and through optional MCP Tasks with the same explicit plan, output, and approval gate; complete reports remain retained while a compact summary authenticates their exact bytes and top-level decision without exceeding the 16 MiB MCP frame | Tool-schema, typed-argument, task allow-list, stale-output, digest-mutation, CLI bridge, synchronous rejection, and task-result E2E regressions cover no-clobber retention and the 128 MiB report/16 MiB response boundary |
| Composite-Action deterministic pipeline runner parity | Root Action opt-in accepts the same closed plan and optional approval gate, retains the fixed `output-dir/deterministic-pipeline-report.json`, and publishes seven outputs only after revalidating report bytes, SHA-256, identities, counts, schema, and decision; valid rejection evidence is published before the final required-approval failure | Action input/output/static-contract, empty-plan compatibility, fixed-path/no-clobber, retained-rejection, stale/digest-mismatch, and output-authentication regressions |
| Closed deterministic pipeline intent compiler | A CLI-only closed intent compiler accepts explicit required/optional role paths relative to the generated output plan's canonical parent (the intent may live elsewhere, but every role source must be a descendant and `..` rebasing is rejected), stable-reads each bounded source, computes descriptor bytes and SHA-256 identities, and emits compact plan-schema-v1 JSON with exactly one trailing newline; the runner binds raw output bytes as `plan_source_sha256` and the semantic plan as `plan_sha256`, without LLM, network, or path discovery; the existing runner remains final authority while MCP/Action compiler parity is deferred | Intent/plan schema, role completeness/null handling, path/link/snapshot bounds, compiler-owned digest and canonical-byte regressions, no-clobber output, and runner compatibility tests; no-network/no-LLM/path-discovery behavior is an implementation-boundary review rather than a direct marker test |
| MCP deterministic pipeline intent compiler parity | The closed compiler is available synchronously and through optional MCP Tasks with explicit intent/output paths, pre-child stale-output rejection, bounded cancellation, and metadata-only intent/plan byte and SHA-256 identities authenticated against stable retained files; plan and role bodies stay outside the MCP response, the runner remains final authority, and Action parity is deferred | Closed tool schema and annotations, task allow-list/lifecycle, typed and unknown arguments, stale-output preservation, strict child-summary parsing, exact intent/plan digest matching, synchronous compilation, Task result, cancellation, and legacy-protocol regressions |
| Composite-Action deterministic pipeline intent compiler parity | The root Action accepts one explicit workspace-relative intent/output pair, rejects partial or mixed legacy-plan selection before analysis, authenticates the compiler's exact bounded intent/plan source identities, passes the resulting effective plan to the unchanged final-authority runner, and after runner EOF rechecks both source identities plus the report's raw plan binding before publishing metadata only | Action schema and environment wiring, compiler-before-analysis ordering, strict summary/canonical-plan verification, stale/link/oversize/change/no-output/nonzero rejection, post-compilation intent/plan substitution rejection, retained runner-rejection evidence, legacy-plan compatibility, and public CI smoke regressions |
| Deterministic firmware-bundle compiler preflight | Before publishing a plan, the compiler stable-preflights the explicit `firmware_manifest` descriptor as `manifest.json` in an exact eight-entry directory (manifest plus the seven fixed v2 source artifacts), rejecting symbolic links, special files, unsafe names, missing/extra entries, malformed or duplicate manifest fields, and every bounded manifest-to-source byte-count/SHA-256 mismatch; the plan remains schema v1 with no source descriptors or artifact hashes, and the compiler does not execute builds, approve build evidence, bind the canonical schematic, or call LLM/network services | Compiler unit and CLI/MCP regressions cover exact-set enumeration, regular-file/symlink/special-file rejection, strict manifest parsing, per-artifact bytes/hash verification, no-clobber/no-output failure, and runner compatibility; the shared runner prescan, final full resnapshot, and public Action success fixture are implementation/CI boundary checks |

| Deterministic firmware-bundle gate re-snapshot | Around the deterministic pipeline gate, the runner reopens all eight exact-entry firmware files and compares complete byte/SHA-256 snapshots immediately before and after the gate, retaining a valid rejected report when either snapshot observes an entry added, removed, replaced, or content-mutated; plan schema v1 and firmware manifest v2 remain unchanged, and regular hardlinks remain allowed/content-bound rather than inode-pinned | Runner unit regressions cover same-size overwrite, rename-replace, pre-gate suppression, and post-gate result retention; existing CLI/MCP/Action compatibility tests remain green, and new MCP compiler regressions cover invalid exact-eight bundles plus stale-output preservation. Hardlink acceptance and adversarial filesystem race freedom are documented boundaries, not claims backed by new tests |
| Deterministic pipeline required CI check | An independent `Deterministic Pipeline` job runs the real Rust binary on normal pull requests and pushes, authenticates compiler source identities and the runner report binding, exercises an accepted chain plus semantic manufacturing rejection, retains ordinary and `--require-approved` rejected reports at separate paths, and publishes only bounded scalar Job Summary values plus scanned artifacts without changing plan/report schema v1; firmware validation remains manifest-evidence checking rather than build replay, and the PR workflow is not an authorization boundary | CI fixture and workflow regressions cover accepted execution, semantic manufacturing rejection, retained normal rejection, nonzero required-approval rejection with separate evidence, compiler source/report binding, bounded summary fields, and artifact scan/retention; required-check branch protection, latest-SHA success before enabling the context, GitHub Actions app binding (`app_id: 15368`), and malicious write-collaborator authorization remain repository-governance boundaries |

## Final verification commands

```sh
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo build --workspace --release
PYTHONPATH=agent/src python3 -m unittest discover -s agent/tests -v
cargo run -p pcbex -- route-kicad examples/multilayer.kicad_pcb \
  --output /tmp/pcbex-complete.kicad_pcb \
  --json-output /tmp/pcbex-complete-ipc.json --drc
cargo run -p pcbex -- fabricate /tmp/pcbex-complete.kicad_pcb \
  --output-dir /tmp/pcbex-complete-mfg
```

<!-- completion-audit:start -->
Version 1.504.0 exposes 1307 Rust tests and 596 Python tests. The release workflow
also verifies formatting, Clippy, release builds, KiCad DRC fixtures, SBOMs,
and build-provenance attestations.
<!-- completion-audit:end -->
